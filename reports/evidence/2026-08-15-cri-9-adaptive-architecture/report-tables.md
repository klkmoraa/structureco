# CRI-9 — Tablas exhaustivas

**Clasificación:** `AUDIT/TEMPORARY`. Generado por `build-cri9-tables.mjs`; no editar a mano.

El informe principal (`reports/2026-08-15-0400-cri-9-arquitectura-interaccion-adaptativa.md`) se lee sin este archivo. Aquí viven las versiones completas de las matrices que allí aparecen resumidas.


## TABLA A — Registro de decisiones D-01…D-15

| ID | Decisión | Status | Qué se decide | Por qué | Evidencia |
|---|---|---|---|---|---|
| D-01 | Contrato real de Medium | **DECIDED** | Las tres clases dejan de ser umbrales de ancho y pasan a ser la SALIDA de una regla de presupuesto. El resolutor elige la composición más rica que cumple CB-1..CB-6: X2 (dos docks laterales), M1 (un dock lateral de iconos + detalle superpuesto sin reflow) o K0 (cero docks, una capa contextual). Medium = M1: ToolRail a 76px permanente, detalle contextual superpuesto que NO recompone el lienzo, y cero superficies de resultados residentes. | Un umbral declarado es exactamente lo que produjo F-01: tres reglas contradictorias sobre --toolbar-w y un inline en AppShellLayout.tsx:48 que gana a :root. Si la clase se calcula, no hay umbral que contradecir. El modelo calibrado devuelve 1024×768 a 81.7% de lienzo en reposo (hoy 24.6%) y mantiene 768×1024 en Compact, que es donde ya estaba bien. | canvas-budget-model.mjs (calibrado a ±0.24 pp contra CRI-7 §2); CRI-7 F-01; CRI-8 §7 (103/122 tareas repiten Expanded). |
| D-02 | Presencia del Inspector | **DECIDED** | La presencia del detalle es contextual a la selección en las TRES clases. Y el Inspector actual se parte por dueño: `detail` (selección), `analysis-setup` (casos, combinaciones, orden, P-Delta) y `view` (capas, snap, filtro de selección). Las rutas de edición individual y múltiple se conservan íntegras: `detail` tiene dos cardinalidades, no dos superficies. | Dos de las tres pestañas del panel de la selección no dependen de la selección (CRI-8 §5.3), y el panel cuesta 27.6% del viewport en 1024×768 — más que el propio lienzo. La decisión no depende de U-02 (tiempo de sesión sin selección): un panel contextual es estrictamente mejor que un panel vacío permanente en todos los viewports medidos, y cuando hay selección el panel está. | Inspector.tsx:86-95; CRI-8 §5.3 y §12.1; CRI-7 §2. |
| D-03 | Descomposición de Results | **DECIDED** | ResultsPanel deja de existir como panel. Sus once responsabilidades se reparten por dueño: (1) estado del análisis → TopBar, global persistente; (2) fiabilidad → chip en TopBar + superficie de causa invocable (D-14); (3) elección de evidencia N/V/M/deformada/heatmap → superficie `view`, porque elegir evidencia es elegir capa; (4) overlay sobre el lienzo → lienzo, gobernado por `view` y por el estado de análisis; (5) detalle del objeto y procedencia → superficie `detail`, junto a sus propiedades; (6) datos densos (reacciones, influencia, aprender) → superficie `dense`, invocada, nunca residente. | El panel reserva 320px de alto ANTES de que exista un resultado (22–25% del viewport). El propio panel ya sabe que es varias cosas: tiene tres modos y agrupa ocho pestañas en cinco familias. RFEM documenta que su navegador de resultados aparece tras un cálculo correcto; ETABS desacopla disponibilidad de visibilidad permanente. El modelo confirma que un dock de datos densos sólo es asumible por encima de ~1700px, y aun así como pin explícito. | ResultsPanel.tsx:40,339,514-520; CRI-8 §5.4; CRI-7 §2; RFEM 6 Navigator; ETABS Table Options. |
| D-04 | Reparto de espacio por defecto y qué puede sobreescribir el usuario | **DECIDED** | El defecto lo calcula el resolutor desde CB. Las preferencias del usuario dejan de ser estado de layout y pasan a ser INTENCIONES (`dockIntent: auto \| pinned \| hidden` por dock, ancho preferido, detent preferido, `focusCanvas`). El resolutor las honra cuando CB lo permite y las DEGRADA — nunca las viola — cuando no. Toda degradación se anuncia por el canal de estado; ninguna se aplica en silencio. Los tres conmutadores solapados actuales (inspectorCollapsed / fullCanvas / toolRailCompact) colapsan a `dockIntent` por dock + un único `focusCanvas`. | La palanca ya está construida y es sólida (clamp 280-480, detents normalizados, persistencia tolerante a fallos). Lo que falla es el defecto y una cascada de tres capas. Además `onToggleInspector` desactiva `fullCanvas` como efecto lateral, un modelo mental que nadie declaró. | useWorkspaceLayoutPreferences.ts:19-85; WorkspaceShell.tsx:246-259; AppShellLayout.tsx:48; CRI-8 §8.4. |
| D-05 | Qué decide el viewport, el contenedor, el input y el contexto | **DECIDED** | Regla única y excluyente: ESPACIO (viewport + visualViewport + safe-area) decide composición y presentación; CONTENEDOR decide el layout interno de cada superficie, siempre con container queries y nunca con @media; INPUT decide afordancias (tamaños de objetivo, gestos, lupa, picker, dependencia de hover) y NUNCA presencia; SELECCIÓN decide presencia y contenido del detalle y de las acciones contextuales; WORKFLOW decide superficies temporales; ANÁLISIS decide disponibilidad de evidencia y datos densos. Regla de implementación: exactamente UN módulo puede leer `matchMedia` — el sensor de entorno. Ningún componente vuelve a preguntarle al navegador por su ancho. | Hoy hay 31 umbrales de ancho distintos en 96 bloques condicionados por ancho, y al menos cuatro dueños distintos de la decisión responsive (styles.css, ResultsPanel con su propio matchMedia, ModelDoctor con su umbral de 700px en JS, WorkspaceShell con el suyo de 1024). Con esa dispersión, tres reglas contradictorias sobre la misma variable pasan desapercibidas — que es literalmente F-01. | ResultsPanel.tsx:83-86,292-305; ModelDoctor.tsx:44-48; WorkspaceShell.tsx:66,126,266; recuento de umbrales verificado en main. |
| D-06 | Selección precisa y objetos solapados en táctil | **DECIDED** | Un solo contrato de cinco fases, idéntico en los cuatro inputs: candidatos → previsualización → elección/ciclado → compromiso → cancelación. La lupa es la FASE DE PREVISUALIZACIÓN y el picker de solapados es la FASE DE ELECCIÓN: son dos fases de un contrato, no dos mecanismos. No se añade un cuarto gesto táctil: el long-press que hoy selecciona a ciegas pasa a ARMAR el picker cuando hay más de un candidato, y se comporta exactamente como hoy cuando hay uno solo. Cancelar nunca altera la selección previa, y `Escape` con el picker abierto cierra sólo el picker (alcance acotado), no los siete estados. | El mejor mecanismo de precisión del producto está apagado justo en el input que más lo necesita: la rama táctil sale antes de llegar al picker para resolver la intención pan/long-press. Y la lupa no se arma durante un tap simple de selección. Onshape documenta el mismo problema («el dedo tapa el objetivo») y lo resuelve con mantener-arrastrar-soltar; no se copia su retícula, se adopta el principio de separar ver de elegir. | StructuralCanvas.tsx:1238-1244 (salida táctil previa al picker), :1245-1283 (picker + ciclado), :1395-1399 (armado de la lupa), :954-978 (long-press 480ms); CRI-8 §10.3. |
| D-07 | Paridad funcional entre métodos de entrada | **DECIDED** | Contrato de paridad: toda tarea debe tener al menos una ruta para {puntero+teclado} y una para {táctil}, salvo que sea intrínsecamente específica de un input. Las cinco brechas verificadas se cierran así: copiar/pegar/duplicar (MOD-13), borrar (MOD-09), repetir (MOD-12) y transformar (MOD-10) → superficie `contextual-actions`, que existe mientras hay selección; marco de selección (SEL-03) → submodo explícito de la herramienta `select`, armado desde `view`/acciones contextuales y desarmado al completar el marco (no un gesto nuevo: dos dedos ya es pinch); pegado de bloque en el datasheet (DAT-06) → afordancia propia en la barra de la tabla que lee el portapapeles asíncrono. | Es un hueco de paridad entre inputs, no evidencia de una segunda arquitectura. La restricción «no crear versión funcional separada por dispositivo» está incumplida hoy en cuatro puntos verificados y en Repeat. | StructuralCanvas.tsx:1241-1244, :1307-1311, :1669-1692; DatasheetPanel.tsx:285-296; CRI-8 §6.3. |
| D-08 | Conflicto y recuperación sin salir de la mesa | **DECIDED** | Nueva superficie `recovery`, invocada desde el chip de persistencia de la TopBar, que lista `listRecoveries(projectId)` con fecha, motivo y checksum, y ofrece restaurar (`restoreRecovery`) o seguir en la revisión viva. Precedencia declarada, no inventada: localStorage es la autoridad de la SESIÓN VIVA (es de donde arranca el proyecto); IndexedDB es la autoridad de la BIBLIOTECA; un conflicto nunca se resuelve solo. No se toca la persistencia ni la política local-first. | El estado `conflict` se anuncia en la mesa y la restauración vive en el Project Hub, dentro de la Welcome: no hay ruta desde el workspace. Es el hueco de acceso más grave del mapa y es pérdida potencial de trabajo. | ProjectContext.tsx:38 (arranque desde localStorage), :87-111 y :161-200 (espejo y bloqueo por conflicto), projectRepository.ts:195-208 (la recuperación se escribe en la MISMA transacción, antes de lanzar el conflicto), :277-297 (migración legacy). Resuelve U-10. |
| D-09 | Responsabilidad del menú «Más» | **DECIDED** | «Más» deja de ser un menú y pasa a ser una REGLA: es el desbordamiento de UNA zona concreta, filtrado por el contexto de esa zona, y nada más. Sus 19 entradas actuales se reparten: historial → permanente en TopBar (nunca en desbordamiento); contexto de análisis → `analysis-setup`; preferencias → `preferences`; exportación → `output`; layout → intenciones del resolutor; diagnóstico → lanzador propio con recuento en las tres clases. Invariante: un elemento sólo puede aparecer en un desbordamiento si además tiene una casa NO-desbordamiento en alguna clase. | Hoy mezcla siete naturalezas en un popover plano, con Deshacer junto a idioma. Shapr3D documenta lo contrario: su `More` sigue filtrado por la selección en lugar de ser un cajón. | TopBar.tsx:473-520; CRI-8 §5.1; CRI-7 F-06; Shapr3D «Accessing tools». |
| D-10 | Un solo modelo de visibilidad del lienzo | **DECIDED** | Dueño único: el estado de vista del lienzo, en la superficie `view`, FUERA de ProjectModel. Las capas/presets y los `show*` del Inspector se fusionan en un solo control. `settings.show*` sale de `ProjectSettings` en la fase de implementación, con migración. Si el coste de esa migración se juzga inasumible, el repliegue declarado es: los `show*` permanecen en el modelo pero la superficie `view` es la ÚNICA ruta de escritura, de modo que el número de dueños en la interfaz siga siendo uno. | La pregunta de U-05 («qué gana si se contradicen») tiene respuesta verificada y no es la que se suponía: NO se contradicen, se componen con AND. `layers.results && layers.labels && resultsAllowed && project.settings.showResultValues && analysis?.success`. Hay dos dueños y ninguno gana; el usuario puede apagar el dibujo desde dos sitios y encenderlo desde uno sin efecto. Además `settings.show*` es estado de PRESENTACIÓN dentro del modelo persistido: abrir el proyecto de otra persona cambia tus preferencias de vista. | StructuralCanvas.tsx:1962, :2403; CanvasResultLayer.tsx:103,223,417-427; editorLayers.ts:39-70; types.ts:184-193. Resuelve U-05. |
| D-11 | Datasheet: destino modal o vista coordinada | **DECIDED** | Las dos cosas, resueltas por la misma regla de presentación que el resto: `dense` es una superficie con tres presentaciones (dock fijable en Expanded muy ancho, drawer en Medium, fullscreen en Compact). El cambio de contrato que importa no es el acoplamiento: es que LOCALIZAR YA NO CIERRA la superficie, la degrada a `peek` y la recuerda, con vuelta explícita. Se añade el modo «sólo la selección» como faceta más del filtrado existente. | Hoy enfocar exige cerrar la tabla, porque el drawer taparía el objeto centrado — una consecuencia honesta de la superficie, no un descuido. `peek` resuelve la causa sin acoplar ventanas. El dock fijable sólo es asumible por encima de ~1700px según el modelo, así que el acoplamiento de RFEM no se importa: se ofrece donde el presupuesto lo paga. «Show Selection Only» de ETABS es barato aquí porque la sincronía selección↔fila ya existe en ambos sentidos. | DatasheetPanel.tsx:359-361 (cierra antes de emitir focus-object), :182-197; canvas-budget-model.mjs (dock denso); ETABS Table Options form; RFEM Tables. |
| D-12 | Una ficha de propiedades o tres | **DECIDED** | Un solo view-model de objeto (lectura) y un solo tipo de intención de edición (escritura), con TRES presentaciones y DOS cardinalidades. `DatasheetEditorPanel` deja de reimplementar la ficha y pasa a alojar la superficie `detail`; Bulk Edit deja de ser una superficie aparte y pasa a ser `detail` cuando `selection.kind === "multi"` — que es donde ya vive físicamente. La duplicación por TAREA (agregación de valores mixtos, recuento de afectados) se conserva; la duplicación por PRESENTACIÓN se retira. | Las tres escriben ya el mismo modelo con `materialId`/`sectionId` explícitos, que es lo difícil y está bien resuelto. Lo que sobra son tres implementaciones de la misma lectura. | CRI-8 §8.3 y §8.2 (INS-03 con cuatro puertas legítimas); BulkEditInspectorPanel.tsx:32-81; identityMetadata.test.ts. |
| D-13 | Contrato borrador → preview → aplicar | **DECIDED** | Hay UN contrato arquitectónico y ya existe en el código, con dos niveles de conformidad. Los cuatro invariantes son obligatorios: (1) `prepare` es puro y ENUMERA lo que hará y lo que no puede hacer; (2) el preview sale de la MISMA función que la escritura; (3) `execute` revalida contra un snapshot del modelo del que se preparó y se niega si el modelo se movió debajo; (4) una sola entrada de historial y una sola invalidación de análisis. Nivel A (objeto `Prepared*` cruzando la frontera de UI): reparación de topología, edición estructural, generación. Nivel B (intención compilada a un único `ProjectCommand`): bulk edit, plan del datasheet. NO se fuerza una implementación común. Lo que SÍ se unifica es el ciclo de vida de UI: todo borrador se registra en `DraftLifecycle` (abierto / sucio / previsualizado / aplicado / cancelado / interrumpido), porque es lo que la matriz de transiciones necesita para garantizar que un resize nunca hace commit ni cancel en silencio. | Las cinco implementaciones son correctas y sus garantías transaccionales difieren; abstraerlas por simetría sería estética. Pero la seguridad ante transiciones sí necesita una respuesta uniforme a «¿hay un borrador sin aplicar y puede migrar?». | ProjectModelContext.tsx:19-70; ProjectContext.tsx:402-436; DatasheetPanel.tsx:238-251; BulkEditInspectorPanel.tsx:66-81; ProjectContext.modelDoctor.test.tsx:104-117. |
| D-14 | Explicación accesible de estados críticos y deshabilitados | **DECIDED** | Contrato «qué / por qué / qué hacer»: todo estado critical, limited, unreliable, stale o disabled expone su causa y su siguiente acción a través de un elemento ENFOCABLE, nunca sólo por `title` ni por hover. Un control que está deshabilitado y tiene causa que comunicar usa `aria-disabled` y sigue siendo enfocable; `disabled` queda sólo para controles sin causa. Los cambios de estado se anuncian por `role="status"`/`aria-live`; la causa se enlaza con `aria-describedby` sobre el propio control. En táctil, tocar el chip abre la superficie de causa. | La causa gobernante de una fiabilidad limitada vive hoy en un `title` de ResultsPanel: no existe en táctil ni por teclado. Es la afirmación más crítica del producto por la ruta más débil. El modelo de tres campos de Model Doctor (qué / por qué / qué hacer) ya existe y funciona: se generaliza en vez de inventarse. | ResultsPanel.tsx:183; CRI-8 §9.2 hueco 5, DOC-03, STA-05; StatusStrip (Brandbook §09). |
| D-15 | Space 3D | **DEFERRED** | Space 3D NO adopta el sistema de composición de CRI-9 y sigue separado y experimental. No se exige paridad 2D↔3D ni superficies ni selección compartidas. Sí adopta DOS contratos neutrales que hoy incumple o duplica: (1) resolución de tema única, en vez de su propio `matchMedia`, porque eso es duplicación de LÓGICA y está explícitamente prohibida; (2) capacidad de input y mínimos de objetivo del proyecto, que hoy viola con controles de 24×44 y 26×36. | Es un dominio, un store y un motor distintos. Importar el sistema de composición exigiría suposiciones espaciales que no existen. Pero un segundo resolutor de tema contradice una restricción protegida, y unos mínimos táctiles propios contradicen los del propio proyecto. | Space3DWorkspace.tsx:50 frente a ProjectContext.tsx:45-49; App.tsx:61-74; CRI-7 §8. |

## TABLA B — Disposiciones G-01, G-02 y F-11

| ID | Asunto | Status | Disposición | Evidencia |
|---|---|---|---|---|
| G-01 | Ctrl+Z / Ctrl+Y anunciados sin manejador | DECIDED — implementar en la fase de implementación, no aquí | Se implementan los atajos, no se retira la promesa. Deshacer y rehacer son globales persistentes y el contrato de paridad (D-07) exige ruta de teclado para una acción global. Regla de alcance obligatoria: el atajo NO dispara cuando el foco está en un campo de texto, en la rejilla del datasheet o dentro de una superficie modal con su propio historial de edición. | CommandPalette.tsx:150,154; sin manejador en src/**; StructuralCanvas.tsx:1642-1734 atiende Ctrl+C/V/D, R, Escape y Delete, no Ctrl+Z. |
| G-02 | `resultTab: "issues"` sin pestaña que lo represente | DECIDED — sale del tipo | `issues` se retira de `ResultTab`. Bajo D-03 un análisis fallido no es una pestaña de resultados: es un estado de análisis (TopBar) más una ruta a Model Doctor. El cuerpo ya renderiza `FailedResults` desde `analysis.success === false`, así que no se pierde nada funcional y se gana que `aria-selected` deje de marcar una pestaña que no refleja el estado. | WorkspaceUIContext.tsx:4; ProjectContext.tsx:239; ResultsPanel.tsx:179,545. |
| F-11 | Cuál de los tres verdes es la fuente única de marca | OWNER DECISION — CRI-9 no elige | Sigue esperando decisión del propietario. Es el único caso donde el propio Brandbook §03 nombra los valores en pugna (#159a72, #00795f, #157A55) y pide elegir uno — y hoy manda un cuarto (#087e5c). CRI-9 no lo toca y CRI-10 no puede empezar la identidad sin él. | brand/brandbook-clay.html §03; tokens.css:61-62,130; public/favicon.svg:2; public/site.webmanifest:11. |

## TABLA C — Catálogo de superficies y presentación por clase

| Superficie | Dueño | Expanded | Medium | Compact | Input | Canvas-budget |
|---|---|---|---|---|---|---|
| `canvas` · Lienzo estructural | selection + domain (lectura) · layout (safe-rect) | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) | Puntero: hover-preview + clic. Táctil: lupa + picker de precisión (contrato PS). Teclado: tabulación por objetos con anillo propio. | Es el presupuesto. CB-1..CB-6 se miden sobre él. |
| `topbar` · TopBar · identidad + acción global + estado | domain (nombre) · analysis (estado) · layout (composición) | band | band | band (identidad comprimida, acciones a iconos de 44px) | Idéntico en los tres inputs. Ningún control depende de hover. | ≈7–9% del viewport; es el único chrome permanente admitido además del rail. |
| `toolrail` · ToolRail · creación y navegación | workflow (herramienta activa) · layout (presentación) | dock 164px con etiquetas | dock 76px sólo iconos (el tier que hoy es código muerto, F-01) | floating — dock horizontal de 5 destinos sobre el lienzo (se conserva tal cual) | Táctil: 44px ya cumplidos. Teclado: 11 teclas de herramienta ya implementadas. | 14.1% → 5.6% al pasar de 164 a 76px en 1024×768. |
| `contextual-actions` · Acciones contextuales de selección | selection | inset — barra anclada al borde del lienzo mientras hay selección | inset | inset — banda sobre el dock de herramientas mientras hay selección | Es la ruta táctil de copiar/pegar/duplicar/repetir/transformar (D-07). Puntero y teclado conservan sus atajos. | Cuenta como chrome flotante (CB-3). Sólo existe con selección, así que nunca compite en reposo. |
| `detail` · Detalle del objeto (propiedades + resultados del objeto) | selection (presencia) · domain (escritura) · analysis (lectura de resultados) | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) | Mismo contenido y mismas escrituras en los tres inputs. Los detents ya existen y ya se normalizan por viewport. | 27.6% del viewport en 1024×768 hoy, permanente. Contextual y en Medium: 0% en reposo, 29% con selección y sin reflow. |
| `analysis-setup` · Contexto de análisis | domain (casos/combinaciones) · analysis (orden, P-Delta) | inset invocado desde el botón Analizar | inset invocado | sheet invocada | Idéntico. Ningún control depende de hover ni de `title`. | Cero en reposo: es invocado, no residente. |
| `view` · Vista del lienzo · capas, evidencia, snap y filtro de selección | layout (estado de vista del lienzo, fuera de ProjectModel) | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada | Los chips informativos del lienzo pasan a accionables (P-11). Táctil y teclado equivalentes. | Cero en reposo. |
| `dense` · Datos densos · datasheet, reacciones, influencia, aprender, procedencia | domain (proyección) · analysis (tablas de resultado) · selection (sincronía fila↔objeto) | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar | Táctil: multiselección por filas y rangos ya existe. Pegado de bloque necesita afordancia propia (D-07). | Cero en reposo. El dock fijable es capacidad, no defecto. |
| `doctor` · Model Doctor · diagnóstico preventivo | domain (lectura) · workflow (reparación preparada) | drawer lateral | drawer lateral con `peek` al localizar | sheet/fullscreen con `peek` al localizar | El lanzador debe llevar recuento de severidad y existir en las tres clases (hoy desaparece bajo 1024px, F-06). | Cero en reposo. |
| `palette` · Command Palette | workflow | overlay modal centrado | overlay modal centrado | sheet a pantalla casi completa (el teclado virtual manda) | Puntero/teclado: `Ctrl/⌘+K`. Táctil: entrada propia; no puede ser la única ruta de nada. | Cero en reposo. |
| `preferences` · Preferencias · unidades, idioma, tema, modo de cálculo | domain (unidades, idioma, modo) · layout (tema) | drawer invocado | drawer invocado | sheet invocada | Idéntico. | Cero. |
| `output` · Salida · exportación, imagen, impresión, memoria | domain (lectura) · analysis (lectura) | drawer invocado desde una única entrada | drawer invocado | sheet invocada | Idéntico. | Cero. |
| `recovery` · Recuperación y conflicto de revisión | domain (persistencia) | drawer invocado desde el chip de persistencia | drawer invocado | sheet invocada | Idéntico. El chip de persistencia es un botón real, no un adorno. | Cero. |
| `transient` · Superficies efímeras · entrada rápida, picker, previews, marco | workflow | overlay sobre el lienzo | overlay | overlay | El picker de solapados se enruta a táctil (D-06). La entrada rápida ya acepta coma decimal. | Chrome flotante mientras dura (CB-3). |
| `status` · Estado transitorio · toasts y anuncios | workflow | overlay | overlay | overlay (por encima del dock, respetando safe-area) | Idéntico; ya está anunciado a lectores de pantalla. | Chrome flotante efímero. |
| `welcome` · Entrada · Welcome y Project Hub | domain | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen | Idéntico. | No compite: no hay lienzo. |
| `classroom` · Guía de Aula | domain (modo) · analysis (progreso) | inset sobre el borde superior del lienzo | inset | inset colapsable a una línea | Idéntico. | Cuenta como chrome flotante en Compact (CB-3). |
| `space3d` · Space 3D (experimental) | dominio propio, fuera de este contrato | fullscreen | fullscreen | fullscreen | Fuera del contrato de paridad 2D. | No compite: sustituye la mesa. |

## TABLA D — Ownership de estado (categorías A–E)

| Categoría | Estado | Dueño | Quién escribe | ¿Persiste? | En una transición | Hoy | Cambio |
|---|---|---|---|---|---|---|---|
| A · Dominio / persistido | Geometría, apoyos, cargas, materiales, secciones, casos y combinaciones | ProjectModelContext (único) | `executeProjectCommand`, `executePrepared*`, `updateProject`, transacción de arrastre | Sí — localStorage (sesión viva) + IndexedDB (biblioteca) | INMUTABLE. Ninguna transición de layout puede tocarlo. | Correcto ya. Un solo ProjectModel; el datasheet es proyección, no modelo paralelo. | Ninguno, salvo la salida de `settings.show*` que decide D-10. |
| A · Dominio / persistido | Unidades, idioma, modo de cálculo, orden de análisis, configuración P-Delta | ProjectModelContext | `updateProjectView` (sin historial) y `updateProjectAnalysisSettings` (invalida análisis) | Sí, dentro del proyecto | Inmutable. | Correcto. La distinción entre ajuste visual y ajuste que invalida el análisis ya está tipada. | Ninguno. Sólo cambia DÓNDE se editan (superficies `preferences` y `analysis-setup`). |
| A · Dominio / persistido | `settings.showGrid`, `showLoads`, `showResultValues`, `showResultOverlay`, … | HOY: ProjectModelContext. OBJETIVO: estado de vista del lienzo (categoría D). | HOY: Inspector › Vista. OBJETIVO: superficie `view`, única. | Hoy dentro del proyecto; objetivo: preferencia de workspace por proyecto | Inmutable en ambos casos. | Es estado de PRESENTACIÓN dentro del modelo persistido: abrir el proyecto de otra persona cambia tus preferencias de vista. | D-10. Migración de esquema; es el mayor riesgo adyacente a schema de CRI-9. |
| A · Dominio / persistido | Historial de deshacer/rehacer | ProjectModelContext (50 entradas acotadas) | Sólo las rutas reversibles declaradas | No | Inmutable. | Correcto. Una operación = una entrada, incluidas las masivas y los arrastres. | Ninguno. Sólo se le añaden los atajos de teclado que ya se anuncian (G-01). |
| B · Análisis | Resultado, éxito/fallo, fiabilidad, check gobernante, obsolescencia | ProjectAnalysisContext | Sólo el worker de análisis y `invalidateAnalysis` | No (se restaura desde el proyecto sólo en `replaceProject`) | Inmutable. Una transición nunca invalida ni relanza el análisis. | La obsolescencia NO es una bandera sobre un resultado vivo: `invalidateAnalysis` pone el análisis a `null`, y `stale` se deriva de `analysis === null && hadAnalysis`. Por eso es IMPOSIBLE pintar evidencia caducada sobre el modelo: todos los overlays exigen `analysis?.success`. Resuelve U-06 y es una propiedad fail-closed estructural que hay que conservar. | Ninguno. |
| C · Selección / contexto | Selección actual (nodo / miembro / carga / multi / null) | WorkspaceUIContext (único) | Lienzo, datasheet, paleta, Model Doctor, tarjetas de resultado — todos por el mismo `setSelection` | No | INMUTABLE por T-INV-1. Ninguna transición de layout la toca. | Es la mayor fortaleza del producto: una sola selección compartida por cinco superficies, sin sincronización explícita. | Ninguno en el estado. Cambia su CONSECUENCIA: pasa a gobernar la presencia del detalle y de las acciones contextuales. |
| C · Selección / contexto | Objeto enfocado (`focus-object`) | Bus `workspaceCommands`, con carga tipada `FocusableSelection` | Cuatro emisores, un consumidor (el lienzo) | No | No se reemite en una transición. | Tipado tal que es imposible pedir un foco inválido. Se conserva íntegro. | Se le añade un token de ORIGEN opcional, para que la navegación profunda pueda volver (§10). Es lo único nuevo que necesita el bus. |
| C · Selección / contexto | Herramienta activa y cursor de resultado | WorkspaceUIContext | Rail, dock, hoja, paleta, teclado — todos sobre el mismo registro de herramientas | No | Conservados. | Registro único de herramientas que alimenta cuatro superficies. Se conserva. | Ninguno. |
| D · Presentación / layout | Entorno (clase de espacio, viewport visual, safe-area, orientación, capacidad de input, movimiento reducido) | NUEVO · Sensor de entorno — el ÚNICO módulo que puede leer `matchMedia` | Nadie: es derivado del navegador | No | Es el disparador de la transición. | Repartido entre CSS (31 umbrales), ResultsPanel, ModelDoctor y WorkspaceShell, cada uno con su propio umbral. | Se centraliza. Es el cambio estructural que hace imposible repetir F-01. |
| D · Presentación / layout | Plan de composición (presentación de cada superficie) | NUEVO · Resolutor — función PURA `(entorno, intenciones, hechos de contexto) → plan` | Nadie: es una función | No — se recalcula siempre | Se recalcula con histéresis y sólo sobre tamaño estable. | No existe: la composición emerge de la cascada CSS. | Nuevo. Al ser puro, se prueba sin DOM: es la parte más testeable de toda la propuesta. |
| D · Presentación / layout | Intenciones del usuario (`dockIntent` por dock, ancho preferido, detent preferido, `focusCanvas`) | NUEVO · LayoutIntent, evolución de `useWorkspaceLayoutPreferences` | El usuario, explícitamente | Sí — localStorage, por dispositivo/perfil, NUNCA dentro del proyecto | Se conservan tal cual. El resolutor puede DEGRADARLAS, jamás reescribirlas. | Existen y funcionan (clamp 280-480, detents normalizados, persistencia tolerante a fallos). Lo que falla es el defecto. | Tres conmutadores solapados → un `dockIntent` por dock + un `focusCanvas`. El ancho deja de escribirse como inline incondicional. |
| D · Presentación / layout | Superficie activa, pila de retorno y propiedad del foco | NUEVO · Broker de superficies (con estado) | Invocaciones de superficie y transiciones | No | Aplica T-INV-2, T-INV-3 y T-INV-8: migrar, no cerrar; foco sigue a su superficie; borrador sucio suspende en vez de destruir. | Repartido en `WorkspaceShell` con cuatro banderas booleanas y tres refs de retorno de foco, todas correctas pero sin contrato común. | Se formaliza lo que ya se hace bien y se le añade la responsabilidad de la migración, que hoy nadie tiene. |
| D · Presentación / layout | Estado de vista del lienzo (capas, presets, evidencia elegida, snap, filtro de selección) | NUEVO tras D-10 · dueño único, fuera de ProjectModel | Sólo la superficie `view` | Sí — ya se persiste hoy en localStorage para las capas | Conservado íntegro. Una transición nunca cambia qué se ve. | Dos dueños que se componen con AND, uno de ellos dentro del modelo persistido. | D-10. |
| E · Efímero / workflow | Borradores sin aplicar (datasheet, bulk edit, edición estructural, generación, reparación) | NUEVO · registro `DraftLifecycle`; el estado interno sigue en cada superficie | Cada superficie sobre su propio borrador | No | SOBREVIVEN SIEMPRE (T-INV-8). Sólo un acto explícito los aplica o los cancela. | Cinco implementaciones correctas y ninguna compartida; nadie sabe globalmente si hay trabajo sin aplicar. | Se registra el ciclo de vida, no la implementación (D-13). Es lo que hace demostrable la matriz de transiciones. |
| E · Efímero / workflow | Picker de candidatos, entrada rápida, marco de selección, corte no fijado, receta de repetición | Estado local del lienzo | El propio lienzo | No | Se cancelan sólo si el gesto que los sostiene se interrumpe; nunca por un cambio de clase. | `Escape` los limpia los siete de una vez, que es correcto como cancelación total. D-06 le añade un alcance acotado cuando el picker está abierto. | Acotar el alcance de Escape cuando hay picker; nada más. |
| E · Efímero / workflow | Previews de generación, edición estructural y reparación (objetos `Prepared*`) | Superficie que los preparó + `DraftLifecycle` | `prepare*` / `executePrepared*` | No | Sobreviven. Y su propia validación de snapshot ya los invalida si el modelo se movió debajo — esa garantía se conserva intacta. | Ya es lo más sólido del producto en esta categoría. | Ninguno en las garantías. Sólo se declaran ante el registro. |
| E · Efímero / workflow | Toasts y anuncios | Canal de estado | Cualquier superficie, por `show-toast` | No | Sobreviven a la transición; se reubican respetando la safe-area. | Correcto, con 66 regiones aria-live ya en el producto. | Pasa a ser también el canal por el que el Broker anuncia una degradación de composición. |

## TABLA E — Matriz de transiciones

| ID | De | A | Disparador | Selección | Superficie | Borrador | Foco | Scroll | Overlays | Lienzo | Escape |
|---|---|---|---|---|---|---|---|---|---|---|---|
| TR-01 | Expanded (X2) | Medium (M1) | Ventana más estrecha o más baja; CB-1 deja de cumplirse con dos docks. | Intacta. | ToolRail dock 164 → dock 76. Detalle dock (reflow) → inset (sin reflow): el lienzo NO se recompone, se descubre. | Sobrevive. El detalle mantiene su borrador y su desplazamiento. | Permanece en el mismo control; si el control desaparece por compactación del rail, va a su equivalente icónico. | Conservado por ancla. | Sin cambio. Las capas y la evidencia elegida no se tocan. | Cámara intacta; safe-rect se recalcula bajo el inset. | Sin efecto: no hay nada que cancelar. |
| TR-02 | Medium (M1) | Compact (K0) | CB-1 deja de cumplirse con un dock lateral. | Intacta. | ToolRail dock → dock flotante. Detalle inset → hoja con detents, por el borde que CB-6 permita. | Sobrevive; el detalle abre en el detent que garantice ver el campo con foco. | Sigue al detalle dentro de la hoja. Si el detalle no estaba presente, no se abre nada. | Conservado por ancla. | Sin cambio. Si dos capas contextuales estaban activas, CB-6 obliga a suspender la de menor prioridad y a anunciarlo. | Cámara intacta. | Cierra la hoja y devuelve el foco al invocador. |
| TR-03 | Compact (K0) | Medium (M1) | Más ancho o más alto disponible. | Intacta. | Hoja → inset conservando su contenido y su desplazamiento. Dock flotante → dock de iconos. | Sobrevive. Nunca se aplica al migrar. | Sigue al detalle. | Conservado por ancla. | Las superficies suspendidas por CB-6 se restauran en el orden en que fueron suspendidas. | Cámara intacta. | Cierra el inset. |
| TR-04 | Medium (M1) | Expanded (X2) | CB-1 se cumple con dos docks. | Intacta. | Inset → dock con reflow. Rail de iconos → rail con etiquetas. | Sobrevive. | Permanece. | Conservado. | Sin cambio. | Recompone su caja; la cámara se conserva y NO se reencuadra sola (reencuadrar movería el modelo bajo el usuario). | Sin efecto. |
| TR-05 | Retrato | Apaisado (y viceversa) | orientationchange / resize. | Intacta. | En Compact, la hoja cambia de borde: inferior en retrato, lateral en apaisado (CB-6). El contenido y el borrador no cambian. | Sobrevive al cambio de borde. | Sigue al mismo campo dentro de la hoja reubicada. | Conservado por ancla (T-INV-6). | Sin cambio. | Cámara intacta; safe-rect se recalcula. | Sin cambio de semántica. |
| TR-06 | Cualquiera | Split-screen o redimensionado continuo | Arrastre continuo del divisor o de la ventana. | Intacta. | Sin recomposición durante el arrastre: el resolutor sólo commitea sobre tamaño estable, con histéresis en la frontera (T-INV-5). | Sobrevive; no se interrumpe la edición durante el arrastre. | Sin movimiento durante el arrastre. | Sin salto. | Sin cambio. | Se redimensiona de forma continua; el reencuadre automático no se dispara. | Sin cambio. |
| TR-07 | Teclado virtual cerrado | Teclado virtual abierto | `visualViewport.resize` / `scroll`. | Intacta. | NO hay cambio de clase (T-INV-4). La hoja se ajusta al viewport visual y su detent se normaliza si el campo con foco quedaría tapado. | Es justo el caso en que hay borrador: sobrevive por construcción. | Se conserva sobre el campo en edición, que debe quedar por encima del teclado. | La superficie desplaza lo mínimo para que el campo con foco sea visible. | Los toasts respetan el nuevo borde inferior y la safe-area. | Su caja no cambia; sólo el rectángulo seguro. | Cierra el teclado antes que la superficie. |
| TR-08 | Puntero fino | Táctil (dispositivo híbrido) y viceversa | Primer evento de `pointerType` distinto. | Intacta. | Sin cambio de composición (T-INV-7). Cambian tamaños de objetivo, se arma o desarma la lupa y el picker pasa a su variante de dedo. | Sin efecto. | Sin movimiento. | Sin efecto. | Las pistas de gesto del badge de modo cambian de vocabulario. | Sin efecto sobre la cámara; sí sobre las áreas de acierto. | Sin cambio. |
| TR-09 | Superficie abriéndose | Transición de clase simultánea | El usuario invoca una superficie mientras el viewport cruza una frontera. | Intacta. | La apertura gana: se resuelve la presentación en la clase DESTINO y la superficie se abre ya en su forma final. Nunca se abre y se cierra. | No aplica todavía. | Va al destino final, una sola vez. | Inicial. | Sin cambio. | Sin cambio. | Cancela la apertura y devuelve el foco al invocador. |
| TR-10 | Mesa 2D | Space 3D y vuelta | Navegación de pantalla. | La selección 2D sobrevive en memoria: `ProjectProvider` está por encima del cambio de pantalla. | `WorkspaceShell` se desmonta por completo. Toda su presentación (superficie abierta, detents, foco) se pierde. | RIESGO DECLARADO: los borradores que viven en estado local de `WorkspaceShell` no sobreviven. Contrato: antes de navegar a Space 3D, `DraftLifecycle` debe pedir confirmación si hay borradores sucios. | Al volver, foco al lienzo 2D. | No conservado. | Las capas del lienzo sobreviven: ya se persisten en localStorage. | La cámara 2D no se conserva. | No aplica. |

## TABLA F — Invariantes de transición

| ID | Regla | Por qué |
|---|---|---|
| T-INV-1 | Un cambio de clase de espacio nunca muta ProjectModel, nunca hace commit ni cancel de un borrador, y nunca cambia la selección. | La selección es el contexto compartido por cinco superficies; si el layout pudiera tocarla, el resize cambiaría el trabajo. |
| T-INV-2 | Una superficie no se CIERRA en una transición: MIGRA de presentación. Cerrar exige un acto explícito del usuario o un conflicto irresoluble, y entonces se anuncia. | Hoy `WorkspaceShell` cierra el inspector móvil al cruzar 1024px y el Model Doctor cierra al cambiar de proyecto. Lo primero es pérdida de contexto silenciosa. |
| T-INV-3 | El foco sigue a su superficie. Si migra, el foco va al elemento equivalente; si no puede mostrarse, vuelve al invocador y se anuncia. | El retorno de foco ya está bien resuelto para Inspector, Datasheet y Doctor; lo que falta es que también cubra la migración, no sólo el cierre. |
| T-INV-4 | El teclado virtual NO es un cambio de clase. La contracción de `visualViewport` ajusta safe-rect, detents y desplazamiento; jamás vuelve a resolver la composición. | Sin esta regla, escribir dentro de una hoja recompondría la aplicación bajo el dedo del usuario. El producto ya distingue `visualViewport` de `innerHeight`. |
| T-INV-5 | El resolutor tiene histéresis en las fronteras de clase y sólo commitea sobre tamaño estable. Un arrastre continuo de split-screen no puede producir una cascada de recomposiciones. | Es la respuesta estructural al redimensionado continuo; sin ella, la frontera calculada oscila y cada oscilación mueve foco y superficies. |
| T-INV-6 | La rotación conserva el desplazamiento por ANCLA (la fila, el hallazgo o el objeto enfocado), no por offset en píxeles. | Un offset en píxeles tras rotar apunta a otro contenido; el ancla apunta al mismo. |
| T-INV-7 | Un cambio de input (ratón conectado a una tablet, o al revés) cambia afordancias de inmediato y NUNCA la composición. | El input decide cómo se toca, el espacio decide qué cabe. Mezclarlos es lo que hace que una tablet de 1024px se comporte como un portátil. |
| T-INV-8 | Un borrador sin aplicar bloquea la sustitución de su superficie: si el destino es exclusivo, la superficie de origen se SUSPENDE con su estado, no se destruye. | Cinco superficies implementan borradores; ninguna puede perder trabajo porque el viewport cruzó un umbral. |

## TABLA G — Ubicación de las 122 tareas de CRI-8

Regla determinista sobre la clase UX primaria + 80 excepciones declaradas, cada una de ellas una decisión de CRI-9.

| ID | Tarea | Dónde vive hoy | Superficie CRI-9 | Origen | Motivo | Expanded | Medium | Compact |
|---|---|---|---|---|---|---|---|---|
| ENT-01 | Elegir por dónde empezar al abrir la app | Pantalla completa propia, fuera del shell del workspace. | `welcome` | decisión CRI-9 | Pantalla de entrada; fuera del shell de la mesa. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-02 | Continuar el proyecto en el que estaba | Botón principal de la Welcome. | `welcome` | decisión CRI-9 | Pantalla de entrada. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-03 | Crear un proyecto en blanco | Welcome + menú desplegable del nombre de proyecto (TopBar). | `welcome` | decisión CRI-9 | Pantalla de entrada + menú de documento en TopBar. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-04 | Abrir un ejemplo / plantilla | Tarjetas de la Welcome + menú de proyecto del TopBar. | `welcome` | decisión CRI-9 | Pantalla de entrada. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-05 | Filtrar plantillas por tipo | Chips en la Welcome. | `welcome` | decisión CRI-9 | Auxiliar de la lista de plantillas. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-06 | Ver y abrir proyectos guardados (Project Hub) | Sólo en la Welcome, tras `Phase2ProjectHub`. | `recovery` | decisión CRI-9 | D-08: abrir otro proyecto gana ruta desde la mesa, además de la Welcome. Cierra el hueco 2 de CRI-8 §9.2. | drawer invocado desde el chip de persistencia | drawer invocado | sheet invocada |
| ENT-07 | Renombrar un proyecto guardado | Sólo en el Hub. | `welcome` | decisión CRI-9 | Gestión de biblioteca; no compite con el lienzo. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-08 | Duplicar un proyecto guardado | Sólo en el Hub. | `welcome` | decisión CRI-9 | Gestión de biblioteca. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-09 | Restaurar una recuperación tras conflicto | Sólo en el Project Hub, dentro de la Welcome. | `recovery` | decisión CRI-9 | D-08: la restauración deja de vivir sólo en el hub. | drawer invocado desde el chip de persistencia | drawer invocado | sheet invocada |
| ENT-10 | Importar JSON / bundle / PDF (Import Center) | Menú de proyecto del TopBar y Welcome. | `transient` | decisión CRI-9 | Import Center es workflow temporal; se invoca desde el menú de documento. | overlay sobre el lienzo | overlay | overlay |
| ENT-11 | Importar geometría desde DXF | SÓLO en la Welcome. | `transient` | decisión CRI-9 | D-07/D-08: importar DXF deja de exigir volver a Inicio. Cierra el hueco 3 de CRI-8 §9.2. | overlay sobre el lienzo | overlay | overlay |
| ENT-12 | Crear un ejercicio de Aula | Botón «Nuevo ejercicio» de la Welcome. | `welcome` | decisión CRI-9 | Aula no se rediseña aquí; su puerta sigue en la Welcome. | fullscreen (pantalla propia, fuera del shell) | fullscreen | fullscreen |
| ENT-13 | Renombrar el proyecto abierto | TopBar, zona documento. | `topbar` | decisión CRI-9 | Identidad del documento: una de las tres naturalezas que la TopBar conserva. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| ENT-14 | Volver a Inicio desde la mesa | Marca del TopBar. | `topbar` | decisión CRI-9 | Identidad del documento. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| ENT-15 | Abrir Space 3D | Botón de icono del TopBar + tarjeta de la Welcome + menú «Más» en Compact. | `space3d` | decisión CRI-9 | D-15: dominio separado. | fullscreen | fullscreen | fullscreen |
| SHL-01 | Ejecutar el análisis | TopBar, extremo derecho, siempre visible. | `topbar` | decisión CRI-9 | Analizar es la acción global por excelencia. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| SHL-02 | Saber en qué estado está el análisis | TopBar (chip) + cabecera del panel de Results (texto de estado). | `topbar` | decisión CRI-9 | D-03: el estado del análisis sube a chip permanente. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| SHL-03 | Deshacer | TopBar (icono), menú «Más» (primer bloque), Command Palette. | `topbar` | decisión CRI-9 | D-09: deshacer nunca vive en un desbordamiento. + G-01 (atajo). | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| SHL-04 | Rehacer | Idéntico a SHL-03. | `topbar` | decisión CRI-9 | D-09 + G-01. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| SHL-05 | Abrir la paleta de comandos | ToolRail + atajo + hoja «Más» móvil. | `palette` | decisión CRI-9 | Aceleración; su lanzador se conserva en el rail. | overlay modal centrado | overlay modal centrado | sheet a pantalla casi completa (el teclado virtual manda) |
| SHL-06 | Navegar a un nudo o barra por identificador | Sólo en la paleta. | `palette` | decisión CRI-9 | Navegación por identificador: ruta de precisión no espacial. | overlay modal centrado | overlay modal centrado | sheet a pantalla casi completa (el teclado virtual manda) |
| SHL-07 | Elegir caso de carga o combinación a analizar | TopBar zona contexto (oculta en Compact) + «Más» + Inspector › Cargas. | `analysis-setup` | decisión CRI-9 | D-09: la combinación viaja junto a los resultados que gobierna, no fija en la TopBar. | inset invocado desde el botón Analizar | inset invocado | sheet invocada |
| SHL-08 | Cambiar el modo de cálculo (Aula / Completo) | CUATRO sitios: TopBar zona contexto, «Más», Inspector › Vista, y el propio guía. | `preferences` | decisión CRI-9 | D-09: preferencia de sesión; pierde tres de sus cuatro puertas. | drawer invocado | drawer invocado | sheet invocada |
| SHL-09 | Elegir el orden de análisis (1.º orden / P-Delta) | TopBar zona contexto + «Más». | `analysis-setup` | decisión CRI-9 | D-09: cambia la física del resultado; baja frecuencia no puede significar escondido. | inset invocado desde el botón Analizar | inset invocado | sheet invocada |
| SHL-10 | Ajustar los parámetros avanzados de P-Delta | SÓLO dentro del popover «Más», bajo un `<details>` colapsado, dentro de una sección. | `analysis-setup` | decisión CRI-9 | D-09: sale de la triple disclosure del cajón. | inset invocado desde el botón Analizar | inset invocado | sheet invocada |
| SHL-11 | Cambiar el sistema de unidades | TopBar zona contexto + «Más» › Preferencias. | `preferences` | decisión CRI-9 | D-09. | drawer invocado | drawer invocado | sheet invocada |
| SHL-12 | Cambiar el idioma | Welcome (cabecera y drawer) + «Más» › Preferencias. NO está en el TopBar de Expanded fuera del cajón. | `preferences` | decisión CRI-9 | D-09. | drawer invocado | drawer invocado | sheet invocada |
| SHL-13 | Cambiar el tema (claro / oscuro) | CUATRO sitios: Welcome, «Más», Command Palette, y Space3D con su propia lógica. | `preferences` | decisión CRI-9 | D-09 + D-15: una sola resolución de tema en todo el producto. | drawer invocado | drawer invocado | sheet invocada |
| SHL-14 | Abrir Model Doctor | Botón propio en TopBar (oculto por CSS bajo 1024px), «Más» (3.ª entrada), paleta, AnalysisStatus, Results fallido. | `doctor` | decisión CRI-9 | D-09: lanzador propio con recuento en las TRES clases; hoy desaparece bajo 1024px (F-06). | drawer lateral | drawer lateral con `peek` al localizar | sheet/fullscreen con `peek` al localizar |
| SHL-15 | Abrir la hoja de datos (Datasheet) | Icono en TopBar + paleta. En Compact el icono NO se oculta (a diferencia de Model Doctor). | `dense` | decisión CRI-9 | D-11. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| SHL-16 | Ocultar / mostrar el Inspector | SÓLO dentro del menú «Más» › Vistas. | `layout-intent` | decisión CRI-9 | D-04: deja de ser una entrada de menú y pasa a ser `dockIntent` del dock de detalle. | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). |
| SHL-17 | Entrar/salir de «mesa completa» | SÓLO «Más» › Vistas. | `layout-intent` | decisión CRI-9 | D-04: `focusCanvas`, único conmutador de enfoque. | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). |
| SHL-18 | Contraer el ToolRail a iconos | SÓLO «Más» › Vistas. | `layout-intent` | decisión CRI-9 | D-04: `dockIntent` del rail; en Medium ya es el defecto calculado. | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). |
| SHL-19 | Redimensionar el Inspector | Borde izquierdo del Inspector. | `layout-intent` | decisión CRI-9 | D-04: el ancho es una intención acotada por el resolutor, no un inline incondicional. | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). |
| SHL-20 | Elegir el detent de la hoja Inspector en táctil | Grupo de tres botones en la cabecera de la hoja. | `layout-intent` | decisión CRI-9 | D-04: el detent preferido es una intención; su normalización por viewport se conserva. | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). |
| SHL-21 | Conocer el estado de persistencia y conectividad | Chip en TopBar (colapsa a icono en Compact) y bloque con descripción dentro de «Más». | `topbar` | decisión CRI-9 | D-08: además es la puerta a la superficie de recuperación. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| SHL-22 | Exportar el proyecto y compartirlo | Menú de exportación (icono propio) en TopBar + sección Exportar de «Más» (las 7 repetidas) + 4 comandos en la paleta. | `output` | decisión CRI-9 | D-09: las 21 afordancias colapsan a una entrada + paleta. | drawer invocado desde una única entrada | drawer invocado | sheet invocada |
| SHL-23 | Recibir avisos transitorios (toasts) | Sobre el lienzo, dentro del `workspace` slot. | `status` | decisión CRI-9 | Canal de estado. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| SHL-24 | Actualizar la app instalada (PWA) | Aviso propio. | `status` | decisión CRI-9 | Aviso de actualización de la PWA. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| MOD-01 | Elegir herramienta activa | ToolRail (164px permanentes en Expanded) + dock flotante de 5 botones en Compact. | `toolrail` | decisión CRI-9 | Crear no puede ser contextual a la selección: es el límite duro del principio Shapr3D. | dock 164px con etiquetas | dock 76px sólo iconos (el tier que hoy es código muerto, F-01) | floating — dock horizontal de 5 destinos sobre el lienzo (se conserva tal cual) |
| MOD-02 | Crear un nudo | Lienzo, con la herramienta activa. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| MOD-03 | Crear una barra | Lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| MOD-04 | Aplicar o cambiar un apoyo | Lienzo (rápido, tipo por defecto) + Inspector (completo) + Datasheet (columna) + Bulk Edit (masivo). | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| MOD-05 | Colocar una carga puntual | Lienzo + hoja «Cargas» del dock táctil + pestaña Cargas del Inspector (que sólo ELIGE la herramienta y cierra). | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| MOD-06 | Colocar una carga distribuida | Lienzo + hoja «Cargas» + Inspector › Cargas. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| MOD-07 | Colocar un momento | Lienzo + hoja «Cargas» + Inspector › Cargas. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| MOD-08 | Dividir una barra (Split) | Rail grupo «Editar» + hoja «Más» táctil + paleta. | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| MOD-09 | Borrar objetos | Rail (destructiva) + tecla + hoja «Más». | `contextual-actions` | decisión CRI-9 | D-07: borrar deja de costar 3 pasos en táctil. | inset — barra anclada al borde del lienzo mientras hay selección | inset | inset — banda sobre el dock de herramientas mientras hay selección |
| MOD-10 | Transformar la selección (mover, rotar, espejo, matriz, alinear, distribuir) | Lanzador `open-structural-edit` que SÓLO aparece en el grupo «Editar» del rail cuando hay selección válida, y en la hoja «Más» táctil bajo la misma condición. | `contextual-actions` | decisión CRI-9 | D-07: ya es el precedente interno (`canEditSelection`). | inset — barra anclada al borde del lienzo mientras hay selección | inset | inset — banda sobre el dock de herramientas mientras hay selección |
| MOD-11 | Entrada numérica rápida durante la colocación | Sobre el lienzo, durante la colocación. | `transient` | regla (clase 6) | Workflow temporal: superficie efímera con ciclo de vida registrado. | overlay sobre el lienzo | overlay | overlay |
| MOD-12 | Repetir la última operación (Repeat) | Tecla `R` + overlay contextual sobre el lienzo. | `contextual-actions` | decisión CRI-9 | D-07: Repeat deja de existir sólo bajo la tecla R (U-03). | inset — barra anclada al borde del lienzo mientras hay selección | inset | inset — banda sobre el dock de herramientas mientras hay selección |
| MOD-13 | Copiar, pegar y duplicar | SÓLO por teclado. No hay botón, entrada de menú ni comando de paleta para copiar/pegar/duplicar. | `contextual-actions` | decisión CRI-9 | D-07: copiar/pegar/duplicar gana su primera afordancia visible y su primera ruta táctil. | inset — barra anclada al borde del lienzo mientras hay selección | inset | inset — banda sobre el dock de herramientas mientras hay selección |
| MOD-14 | Generar una estructura completa | Botón propio en el grupo «Crear» del rail + hoja «Más» › Crear + comando de paleta. | `toolrail` | decisión CRI-9 | Generar crea geometría: no depende de la selección. El código ya lo razona. | dock 164px con etiquetas | dock 76px sólo iconos (el tier que hoy es código muerto, F-01) | floating — dock horizontal de 5 destinos sobre el lienzo (se conserva tal cual) |
| MOD-15 | Medir con la herramienta de dimensión | Rail grupo «Inspeccionar» + hoja «Más» + paleta. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| MOD-16 | Hacer un corte para leer valores internos | Rail grupo «Inspeccionar» + hoja «Más» + paleta. | `canvas` | decisión CRI-9 | El corte vive sobre el lienzo; su precondición (análisis correcto) debe comunicarse por D-14. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| SEL-01 | Seleccionar un objeto | Lienzo (clic/tap), Datasheet (fila), paleta (por ID), Model Doctor (localizar), tarjetas de Results (objeto crítico). | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| SEL-02 | Resolver objetos solapados con puntero | Lienzo, automático. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| SEL-03 | Seleccionar con marco (box select) | Lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| SEL-04 | Multiselección acumulativa | Lienzo (Shift+clic, marco) + Datasheet (filas, rango). | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| SEL-05 | Deseleccionar | Escape + clic en el fondo. | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| SEL-06 | Filtrar qué tipos son seleccionables | Inspector › Vista › sección «Precisión CAD», tras `selectionDragHelp`. | `view` | decisión CRI-9 | D-10: el filtro de selección es visibilidad del lienzo, no una preferencia. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| SEL-07 | Precisión táctil: ver bajo el dedo | Automática sobre el lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-01 | Encuadrar, acercar y alejar | Chrome flotante sobre el lienzo, esquina. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-02 | Orientarse con el minimapa | Esquina del lienzo, siempre. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-03 | Controlar qué capas se ven | Disparador flotante sobre el lienzo (`canvas-layer-trigger`) + 5 comandos de preset en la paleta. | `view` | decisión CRI-9 | D-10: dueño único de la visibilidad. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| CNV-04 | Activar rejilla y ajuste (snap) | Inspector › Vista (los cinco destinos) + comandos `view:grid` y `view:snap` en la paleta + chips de estado sobre el lienzo (SÓLO informativos, no accionables). | `view` | decisión CRI-9 | D-10 + P-11: los chips informativos pasan a accionables. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| CNV-05 | Leer la posición y la escala actuales | Chrome de estado del lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-06 | Saber en qué modo está el lienzo | Esquina superior del lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-07 | Navegar el modelo con el teclado | El propio lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-08 | Arrastrar un nudo | Lienzo. | `canvas` | regla (clase 4) | Canvas-local: vive en el lienzo o en su superficie de vista. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| CNV-09 | Exportar el lienzo como imagen | Menú de exportación + «Más» + paleta. | `output` | decisión CRI-9 | D-09. | drawer invocado desde una única entrada | drawer invocado | sheet invocada |
| CNV-10 | Ver el modelo en modo impresión | Entrada «Imprimir» en el menú de exportación y en «Más». | `output` | decisión CRI-9 | D-09. | drawer invocado desde una única entrada | drawer invocado | sheet invocada |
| INS-01 | Ver y editar las propiedades del objeto seleccionado | Panel lateral de 320px SIEMPRE presente en Expanded, aunque no haya selección. Hoja inferior en Compact. | `detail` | regla (clase 5) | Inspector/detail: ficha del objeto, una sola implementación. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| INS-02 | Aplicar un material de catálogo | Inspector, sección de miembro. | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| INS-03 | Aplicar una sección de catálogo | Inspector. | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| INS-04 | Editar varios objetos a la vez (Bulk Edit) | Dentro del Inspector, sustituyendo la ficha individual. | `detail` | regla (clase 3) | Contextual a selección: presencia gobernada por la selección. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| INS-05 | Gestionar casos de carga y combinaciones | Inspector › pestaña Cargas. Es la ÚNICA puerta a crear casos y combinaciones. | `analysis-setup` | decisión CRI-9 | D-02: casos y combinaciones no dependen de la selección; salen del panel de la selección. | inset invocado desde el botón Analizar | inset invocado | sheet invocada |
| INS-06 | Ajustar la visualización del modelo y de los resultados | Pestaña «Vista» del Inspector — 4 secciones y ~20 controles. | `view` | decisión CRI-9 | D-02 + D-10: los ~20 ajustes de visualización son estado de vista del lienzo. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| INS-07 | Elegir la herramienta de carga desde el Inspector | Inspector › Cargas, sección «Añadir carga». | `toolrail` | decisión CRI-9 | D-09: cinco lanzadores para tres herramientas; sobrevive el del rail y el dock táctil. | dock 164px con etiquetas | dock 76px sólo iconos (el tier que hoy es código muerto, F-01) | floating — dock horizontal de 5 destinos sobre el lienzo (se conserva tal cual) |
| RES-01 | Leer el resumen del análisis | Panel Results, familia «Estado». | `dense` | decisión CRI-9 | D-03: el resumen es dato denso invocado. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| RES-02 | Leer reacciones y localizar la máxima | Panel Results, familia «Estado». | `dense` | decisión CRI-9 | D-03: tabla de reacciones. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| RES-03 | Leer los diagramas N, V y M | Panel Results + overlay sobre el lienzo (capa `results`). | `view` | decisión CRI-9 | D-03: elegir N/V/M es elegir capa; el detalle del miembro va a `detail`. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| RES-04 | Ver la deformada | Panel Results + overlay en el lienzo. | `view` | decisión CRI-9 | D-03: la deformada es una capa. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| RES-05 | Consultar el índice elástico estimado | Tarjeta dentro de Results; capa `heatmap` del lienzo (apagada por defecto). | `view` | decisión CRI-9 | D-03: el mapa de demanda es una capa; su valor por objeto va a `detail`. | inset invocado desde el disparador flotante del lienzo | inset invocado | sheet invocada |
| RES-06 | Entender la fiabilidad del resultado | Texto de estado en la cabecera de Results (con `title` = mensaje del check gobernante) + chip del TopBar + tarjeta de calidad numérica. | `topbar` | decisión CRI-9 | D-03 + D-14: la fiabilidad es la afirmación más crítica; chip permanente + causa invocable enfocable. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| RES-07 | Ver líneas de influencia | Pestaña de Results. | `dense` | decisión CRI-9 | D-03: líneas de influencia piden superficie propia. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| RES-08 | Aprender por qué salió ese resultado | Pestaña de Results, disponible en AMBOS modos (no sólo Aula). | `dense` | decisión CRI-9 | D-03: «Entender» es la vista más larga del panel. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| RES-09 | Cambiar la altura y el modo del panel de resultados | Cabecera del panel (`results-mode-control`) + tirador + botón de enfoque en móvil. | `layout-intent` | decisión CRI-9 | D-04: el modo y la altura del panel desaparecen con el panel; queda el pin del dock denso. | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). | Intención de layout, no superficie: lo consume el resolutor (D-04). |
| RES-10 | Saber de dónde sale un número (procedencia) | Tarjeta al pie del cuerpo de Results. | `detail` | decisión CRI-9 | D-03: la procedencia se lee junto al objeto y su cursor, no al pie de un panel. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| RES-11 | Localizar el objeto de un resultado en el modelo | Cuatro emisores, un solo consumidor (el lienzo). | `canvas` | decisión CRI-9 | Destino del contrato de navegación profunda; `focus-object` se conserva tipado. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| DAT-01 | Ver el modelo como tabla | `Drawer` inferior modal sobre el lienzo. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-02 | Buscar dentro de la tabla | Campo `type="search"` en la barra de la tabla. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-03 | Filtrar y ordenar | Chips bajo la barra de la tabla. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-04 | Seleccionar filas y sincronizar con el lienzo | Rejilla de la tabla. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-05 | Editar una celda | Rejilla. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-06 | Pegar un bloque de celdas | Ctrl+V sobre la rejilla. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-07 | Revisar, aplicar o cancelar un cambio pendiente | Panel lateral dentro del drawer. | `transient` | regla (clase 6) | Workflow temporal: superficie efímera con ciclo de vida registrado. | overlay sobre el lienzo | overlay | overlay |
| DAT-08 | Editar el objeto enfocado desde el panel editor | Panel lateral del drawer. | `detail` | decisión CRI-9 | D-12: el panel editor deja de reimplementar la ficha y aloja la superficie `detail`. | dock 320px con reflow · presencia contextual a la selección | inset 300px sin reflow · presencia contextual a la selección | sheet con detents · lado inferior en retrato, lateral en apaisado (CB-6) |
| DAT-09 | Cambiar de entidad (nudos / barras / cargas) | Barra de la tabla. | `dense` | regla (clase 9) | Datos densos / fullscreen: invocada, nunca residente. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| DAT-10 | Enfocar en el lienzo el objeto de la fila | Botón del panel editor. | `canvas` | decisión CRI-9 | D-11: localizar degrada la tabla a `peek`, no la cierra. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| DOC-01 | Diagnosticar el modelo antes de analizar | `Drawer` lateral en escritorio, INFERIOR bajo 700px (`useCompactDoctor`). | `doctor` | decisión CRI-9 | Superficie propia bien resuelta; sólo cambia su acceso y su umbral. | drawer lateral | drawer lateral con `peek` al localizar | sheet/fullscreen con `peek` al localizar |
| DOC-02 | Filtrar hallazgos por severidad | Cabecera del drawer. | `doctor` | decisión CRI-9 | Filtro dentro de su superficie. | drawer lateral | drawer lateral con `peek` al localizar | sheet/fullscreen con `peek` al localizar |
| DOC-03 | Entender un hallazgo | Tarjeta del drawer, con disclosure. | `doctor` | decisión CRI-9 | El modelo qué/por qué/qué hacer se generaliza en D-14. | drawer lateral | drawer lateral con `peek` al localizar | sheet/fullscreen con `peek` al localizar |
| DOC-04 | Localizar el objeto de un hallazgo | Botón de la tarjeta. | `canvas` | decisión CRI-9 | D-11: localizar degrada el Doctor a `peek`. | layer (protagonista, reflow con el dock de detalle) | layer (protagonista, sin reflow; safe-rect se reduce bajo el inset) | layer (protagonista; safe-rect se reduce bajo la hoja) |
| DOC-05 | Previsualizar y aplicar una reparación de topología | Vista sustitutiva dentro del drawer, con botón de volver. | `transient` | decisión CRI-9 | D-13: reparación preparada, nivel A del contrato. | overlay sobre el lienzo | overlay | overlay |
| DOC-06 | Reconocer (acknowledge) un hallazgo | Botón de la tarjeta. | `doctor` | decisión CRI-9 | Reconocer un hallazgo. | drawer lateral | drawer lateral con `peek` al localizar | sheet/fullscreen con `peek` al localizar |
| DOC-07 | Saltar del hallazgo a la herramienta que lo arregla | Botón de la tarjeta. | `contextual-actions` | decisión CRI-9 | Saltar del hallazgo a la herramienta que lo arregla. | inset — barra anclada al borde del lienzo mientras hay selección | inset | inset — banda sobre el dock de herramientas mientras hay selección |
| PER-01 | Guardar automáticamente el trabajo | Invisible; su estado se ve en el chip del TopBar (SHL-21). | `topbar` | regla (clase 1) | Global persistente: sólo lo global y frecuente tiene permanencia. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| PER-02 | Trabajar sin conexión | Estado en el TopBar. | `topbar` | regla (clase 1) | Global persistente: sólo lo global y frecuente tiene permanencia. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| PER-03 | Resolver un conflicto de revisión | Estado `conflict` en el TopBar; la resolución está en el Project Hub (ENT-09), en otra pantalla. | `recovery` | decisión CRI-9 | D-08: el hueco de acceso más grave del mapa. | drawer invocado desde el chip de persistencia | drawer invocado | sheet invocada |
| PER-04 | Migrar un proyecto de una versión anterior | Invisible, automático. | `topbar` | regla (clase 1) | Global persistente: sólo lo global y frecuente tiene permanencia. | band | band | band (identidad comprimida, acciones a iconos de 44px) |
| STA-01 | Entender un estado vacío | Dentro de cada superficie. | `status` | regla (clase T) | Transversal: canal de estado y anuncios. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| STA-02 | Entender un estado de carga | En cada superficie que carga. | `status` | regla (clase T) | Transversal: canal de estado y anuncios. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| STA-03 | Entender un análisis obsoleto (stale) | Chip del TopBar + estado de Results. | `status` | regla (clase T) | Transversal: canal de estado y anuncios. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| STA-04 | Entender un análisis fallido | Cuerpo de Results + chip del TopBar. | `status` | regla (clase T) | Transversal: canal de estado y anuncios. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| STA-05 | Entender un control deshabilitado | Disperso por superficie. | `status` | regla (clase T) | Transversal: canal de estado y anuncios. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| STA-06 | Trabajar con movimiento reducido / alto contraste | Automático. | `status` | regla (clase T) | Transversal: canal de estado y anuncios. | overlay | overlay | overlay (por encima del dock, respetando safe-area) |
| AUL-01 | Seguir el recorrido guiado de un ejercicio | Banda sobre el lienzo, encima del canvas. | `classroom` | decisión CRI-9 | Aula no se rediseña; sólo se declara su presentación. | inset sobre el borde superior del lienzo | inset | inset colapsable a una línea |
| AUL-02 | Ver niveles pedagógicos del resultado | Results › Entender. | `dense` | decisión CRI-9 | Niveles pedagógicos del resultado: dato denso. | drawer inferior · dock fijable sólo si CB-1 y CB-4 lo permiten (≈≥1700px) | drawer con estado `peek` al localizar | fullscreen con estado `peek` al localizar |
| AUL-03 | Mantener la sesión de aula | Invisible. | `classroom` | decisión CRI-9 | Sesión de aula. | inset sobre el borde superior del lienzo | inset | inset colapsable a una línea |
| S3D-01 | Trabajar en el dominio espacial experimental | Pantalla completa propia, con dos orígenes (Inicio o mesa). | `space3d` | decisión CRI-9 | D-15. | fullscreen | fullscreen | fullscreen |
| S3D-02 | Navegar el modelo espacial por lista | Panel dentro de la pantalla Space3D. | `space3d` | decisión CRI-9 | D-15. | fullscreen | fullscreen | fullscreen |

## TABLA H — Brechas de paridad táctil y cómo se cierran

Lista declarada, no detectada por patrón: son las que CRI-8 verificó una por una en el código.

| ID | Tarea | Tipo | Táctil hoy | Superficie CRI-9 | Cómo se cierra |
|---|---|---|---|---|---|
| SEL-02 | Resolver objetos solapados con puntero | brecha completa | NO DISPONIBLE — hueco explícito y verificado en `StructuralCanvas.tsx:1241-1244`. | `canvas` | D-06: el long-press arma el picker cuando hay más de un candidato. Sin gesto nuevo. |
| SEL-03 | Seleccionar con marco (box select) | brecha completa | NO DISPONIBLE. La ruta táctil a multiselección es el Datasheet o taps acumulativos. | `canvas` | D-07: submodo explícito de marco en la herramienta `select`, armado y desarmado por acción. |
| MOD-12 | Repetir la última operación (Repeat) | brecha completa | UNKNOWN: no se verificó una ruta táctil independiente del teclado. | `contextual-actions` | D-07: Repeat gana casa en `contextual-actions` y entrada de paleta. |
| MOD-13 | Copiar, pegar y duplicar | brecha completa | NO EXISTE. Hueco funcional real en Compact táctil. | `contextual-actions` | D-07: copiar/pegar/duplicar en `contextual-actions`, su primera afordancia visible. |
| DAT-06 | Pegar un bloque de celdas | brecha completa | UNKNOWN: no se verificó una ruta táctil de pegado. | `dense` | D-07: afordancia de pegado en la barra de la tabla, con portapapeles asíncrono. Sujeta a U-11. |
| SHL-20 | Elegir el detent de la hoja Inspector en táctil | brecha parcial | Tap. NO hay arrastre del tirador para cambiar detent. | `layout-intent` | P-12 (CRI-10): el detent debe responder también al arrastre del tirador, no sólo a tres botones. |

## TABLA I — Canvas-budget calculado por el resolutor

| Viewport | Hoy | Lienzo hoy % | Resuelve | Reposo % | Con detalle % | Lienzo | Hoja | Dock denso fijable |
|---|---|---|---|---|---|---|---|---|
| 320×568 · móvil mínimo | Compact | 71.8 | **K0** | 81.3 | 32.6 | 320×185 | bottom | no |
| 360×800 · móvil | Compact | 80 | **K0** | 86.8 | 34.8 | 360×278 | bottom | no |
| 375×667 · móvil | Compact | 76 | **K0** | 84.1 | 33.6 | 375×224 | bottom | no |
| 390×844 · móvil | Compact | 81 | **K0** | 87.4 | 35 | 390×295 | bottom | no |
| 844×390 · móvil apaisado | Compact | 69.5 | **K0** | 83.6 | 51.9 | 524×326 | side | no |
| 768×1024 · tablet retrato | Compact | 82.8 | **K0** | 88.1 | 35.3 | 768×361 | bottom | no |
| 900×1000 · split-screen | Compact | 82.4 | **M1** | 83.3 | 53 | 524×910 | — | no |
| 1024×1366 · tablet grande retrato | — | — | **M1** | 86.5 | 59.1 | 648×1276 | — | no |
| 1024×768 · portátil corriente | Expanded | 24.6 | **M1** | 81.7 | 55.9 | 648×678 | — | no |
| 1112×834 · tablet apaisada | — | — | **X2** | 76.1 | 50.4 | 628×744 | — | no |
| 1180×820 · tablet grande apaisada | — | — | **X2** | 76.7 | 52.5 | 696×730 | — | no |
| 1280×800 | Expanded | 30.3 | **X2** | 77.4 | 55.2 | 796×710 | — | no |
| 1366×768 | — | — | **X2** | 77.7 | 57 | 882×678 | — | no |
| 1440×900 | Expanded | 36.1 | **X2** | 79.8 | 59.8 | 956×810 | — | no |
| 1536×960 | Expanded | 39.2 | **X2** | 80.9 | 62.1 | 1052×870 | — | no |
| 1920×1080 | — | — | **X2** | 83.8 | 68.6 | 1436×990 | — | sí |
| 2560×1440 | — | — | **X2** | 87.7 | 76 | 2076×1350 | — | sí |

## TABLA J — Calibración del modelo contra CRI-7 §2

| Viewport | Tier | CRI-7 medido % | Modelo % | Δ (pp) |
|---|---|---|---|---|
| 320×568 | compact | 71.8 | 71.8 | +0.03 |
| 360×800 | compact | 80.0 | 80.0 | +0.00 |
| 375×667 | compact | 76.0 | 76.0 | +0.01 |
| 390×844 | compact | 81.0 | 81.0 | +0.04 |
| 844×390 | compact | 69.5 | 69.7 | +0.24 |
| 768×1024 | compact | 82.8 | 82.8 | +0.01 |
| 900×1000 | compact | 82.4 | 82.4 | -0.00 |
| 1024×768 | expanded | 24.6 | 24.6 | -0.02 |
| 1280×800 | expanded | 30.3 | 30.3 | +0.02 |
| 1440×900 | expanded | 36.1 | 36.1 | +0.05 |
| 1536×960 | expanded | 39.2 | 39.2 | +0.04 |

## TABLA K — Matriz competitiva

| Producto | Patrón oficial verificado | Re-verificado en CRI-9 | Problema de StructureCo | Aplicación | Fortaleza a preservar | Riesgo de copiar literal | Decisiones |
|---|---|---|---|---|---|---|---|
| Shapr3D | La interfaz adaptativa recomienda herramientas a partir de lo preseleccionado; si la herramienta buscada no aparece, `More` da acceso a las demás funciones VÁLIDAS PARA ESA SELECCIÓN. | sí | ToolRail y TopBar alojan acciones que sólo son válidas con selección, y el menú «Más» mezcla 19 entradas que no dependen de nada. | Superficie `contextual-actions`: las acciones de selección salen del rail y del cajón y aparecen junto a la selección. El desbordamiento pasa a estar filtrado por contexto (D-09). | La selección ya es única y compartida por cinco superficies, y `MOD-10` ya condiciona su lanzador a `canEditSelection`. El precedente interno existe. | Creación y navegación NO pueden ser contextuales: cuando se crea, no hay selección. El propio código ya razona esto para el generador. Un producto que sólo muestra lo válido-para-la-selección deja al usuario sin punto de partida en un lienzo vacío. | D-07, D-09 |
| Onshape | Precision Selector: mantener pulsado con un dedo en el área gráfica hace aparecer un selector que se arrastra como cursor y se suelta sobre la entidad deseada; el motivo documentado es que el dedo tapa el objetivo. `Select Other` recorre las entidades bajo el puntero, incluidas las ocultas, con la tecla ` para bajar por la lista y Shift+` para subir. | sí | El picker de solapados está apagado en táctil y la lupa no se arma durante un tap simple de selección. | Contrato de precisión de cinco fases (D-06): la lupa es la previsualización, el picker es la elección. El long-press que hoy selecciona a ciegas pasa a armar el picker cuando hay más de un candidato. | La lupa clona el render real con `<use>` en vez de redibujarlo, así que no puede desincronizarse. Y el picker de StructureCo es lista Y ciclado con contador «i de n», donde `Select Other` es lista con ciclado por teclado. | Copiar la retícula añadiría un cuarto gesto táctil a un lienzo que ya distingue tap, long-press, arrastre-pan y pinza. La lección no es la retícula: es aceptar que la misma función necesita distinta interacción por método de entrada. | D-06, D-07 |
| Autodesk Fusion | Las pestañas contextuales sólo se activan al invocar su comando y no son visibles hasta entonces. Los entornos contextuales van un paso más allá: SUSTITUYEN las pestañas por defecto del workspace hasta que se sale del entorno. | sí | TopBar y ToolRail exponen a la vez funciones de modelar, revisar resultados, diagnosticar y configurar. | Dos grados formalizados en el catálogo de presentaciones: aparecer JUNTO a lo existente (`inset` de acciones contextuales) o SUSTITUIRLO (estado que ya usan el preview de reparación y la revisión del datasheet). | StructureCo no separa el modelo por fases: se puede analizar mientras se modela y volver sin cambiar de workspace. Esa continuidad es una ventaja real para el usuario y especialmente para Aula. | Crear workspaces por fase fragmentaría un flujo hoy continuo y multiplicaría el coste de ir y volver entre modelar y leer resultados — justo lo contrario de lo que necesita el bucle de aprendizaje. Se adopta el grado contextual; se rechaza el workspace por fase. | D-03, D-09, §11 |
| Dlubal RFEM 6 | El Navigator tiene tres pestañas — Data, Display, Views — y una CUARTA, Results, que aparece tras un cálculo correcto. Las tablas se acoplan y desacoplan del marco de la ventana como el propio Navigator. | sí | Results es un panel único que reserva 320px de alto ANTES de que exista un resultado, y hay dos sistemas de visibilidad sin relación. | Separar Datos / Visualización / Resultados como responsabilidades con dueño distinto (D-03) y que las superficies de resultados aparezcan tras calcular en vez de reservar espacio antes. | La procedencia por número (magnitud, objeto, caso, convención de signos y posición): no se documenta nada equivalente en RFEM. Y la tabla de StructureCo usa la selección del workspace, no una propia. | El árbol del Navigator y el modelo de ventanas acoplables son de escritorio dedicado. Copiarlos rompería Compact, que es donde StructureCo hoy es mejor. El modelo de presupuesto confirma que un dock de datos densos sólo se paga por encima de ~1700px. | D-03, D-10, D-11 |
| RISA-3D | Las toolbars disponibles dependen de la ventana activa; el Explorer lista las hojas de datos y, con solución presente, añade las de resultados. Varias puertas documentadas para la misma función. | no (se cita como en CRI-8) | Validar que varias puertas al mismo estado son fortaleza, y distinguirlas de las ocho duplicaciones innecesarias del mapa. | Disponibilidad de controles dependiente del contexto activo como patrón maduro, no excentricidad. Refuerza que las superficies de resultados aparezcan sólo cuando hay solución. | Un solo `Selection` compartido y un comando `focus-object` tipado que hace imposible pedir un foco inválido. RISA tiene muchas puertas; StructureCo tiene muchas puertas AL MISMO ESTADO, que es más difícil y más valioso. | RISA multiplica puertas sin criterio publicado de cuándo sobra una. Imitar «más puertas» empeoraría exactamente las ocho duplicaciones que D-09 y D-12 vienen a retirar. | D-09, D-12 |
| SkyCiv Structural 3D | Los datos se introducen por formularios, Datasheet y herramientas gráficas; el Datasheet se abre desde el icono del propio formulario. `Repair Model` categoriza los problemas y ofrece RESALTAR los componentes problemáticos antes de actuar. | no (se cita como en CRI-8) | Localizar un objeto exige cerrar el Model Doctor o la tabla, porque taparían el objeto centrado. | El estado `peek`: el diagnóstico resalta y se reduce, no se cierra (D-11). Y abrir la tabla desde la ficha del objeto, no sólo desde el chrome global. | `prepareTopologyRepair` enumera lo que NO puede resolver y lo que omite, detecta si el modelo cambió bajo el preview y bloquea el aplicar; `finding.repair.available === false` produce «reparación ambigua», no una corrección silenciosa. | EL MAYOR RIESGO DE TODA LA MATRIZ. Una reparación automática que no declara sus límites contradice la política fail-closed. Se adopta el resaltado; NO la disposición a arreglar sin enumerar lo dudoso. | D-11, §10 |
| ETABS (CSI) | En el formulario Table Options, «Show Selection Only» muestra los datos de los objetos seleccionados ANTES de invocar el comando, y «Show Only if Used in Model» evita que se incluyan columnas de datos irrelevantes. Las tablas se despliegan en la banda inferior desde Display › Show Tables o desde el Model Explorer. La base admite cientos de tipos de tabla sin mostrarlos permanentemente. | sí | La tabla muestra siempre el modelo entero, y conviven tres mecanismos distintos de «enséñame menos». | Modo «sólo la selección» como una faceta más del filtrado existente (D-11), barato porque la sincronía selección↔fila ya es bidireccional. Y el principio general: capacidad disponible no implica control siempre visible. | El desacoplamiento ya está hecho en parte: el datasheet es un drawer bajo demanda, no un panel residente, y sus facetas se calculan del contenido real con recuento. | ETABS acepta cientos de tablas y un explorador denso como precio de la potencia. StructureCo no debe adoptar esa complejidad como objetivo: lo único que se estudia es cómo desacopla lo disponible de lo permanentemente visible. | D-03, D-11 |

### Patrones que NO se copian

1. Reparación automática sin enumerar lo dudoso (SkyCiv). Contradice fail-closed; StructureCo ya lo hace mejor.
2. Árbol de navegación denso tipo RFEM/ETABS. Rompería Compact, que es donde StructureCo es más fuerte.
3. Workspaces por fase (Fusion). Fragmentaría el bucle continuo modelar↔analizar y perjudicaría a Aula.
4. Ventanas acoplables/flotantes (RFEM). Introduce un modelo de ventanas sin equivalente táctil.
5. Multiplicar puertas sin criterio (RISA). StructureCo ya tiene ocho duplicaciones innecesarias.
6. La retícula de precisión de Onshape como gesto literal. Se adopta el principio (ver ≠ elegir), no el cuarto gesto.
7. Estética, color, radios, sombras, motion, spacing e iconografía de cualquiera de los siete. El Brandbook Clay manda.


## TABLA L — Unknowns

| ID | Pregunta | Status | Efecto sobre las decisiones | Qué haría falta |
|---|---|---|---|---|
| U-01 | No hay telemetría: 86 de 122 frecuencias son inferencia, no medida. | **ABIERTO** | Ninguna decisión de este informe se apoya en frecuencia. La presencia se decide por DUEÑO y por PRESUPUESTO, no por uso estimado. Criticidad y frecuencia son dimensiones distintas y aquí manda la criticidad. | Telemetría local anónima de invocación por superficie, si el propietario la autoriza. Hasta entonces, ninguna cifra de frecuencia puede justificar esconder nada. |
| U-02 | ¿Cuánto tiempo de sesión transcurre sin selección? | **NO BLOQUEANTE** | Se planteaba como condición para D-02 y ya no lo es: un dock contextual es estrictamente mejor que un panel vacío permanente en todos los viewports medidos, y cuando hay selección el dock está. U-02 afinaría el defecto de `dockIntent`, no la decisión. | Misma telemetría que U-01. |
| U-03 | Ruta táctil de Repeat (MOD-12). | **RESUELTO — no existe** | Confirmado: sólo se activa con la tecla `R` sobre una selección repetible; no está en la paleta ni en «Más». D-07 le da casa en `contextual-actions`. | — |
| U-04 | ¿El tap táctil acumula o reemplaza en multiselección? | **RESUELTO — reemplaza** | Un tap táctil sobre geometría entra en `startPending`; al confirmarse ejecuta `selectStructuralTarget`, que REEMPLAZA. La acumulación sólo existe con `shiftKey` (puntero) o con marco aditivo — y el marco no tiene ruta táctil. Es decir: hoy NO hay multiselección táctil sobre el lienzo; la única ruta táctil a multiselección es el datasheet. D-07 lo cierra. | — |
| U-05 | ¿Qué gana si las capas del lienzo y los `show*` del Inspector se contradicen? | **RESUELTO — ninguno: se componen con AND** | No hay precedencia porque no hay competencia: la condición de dibujo es una conjunción. `layers.results && layers.labels && resultsAllowed && project.settings.showResultValues && analysis?.success`. Cualquiera de los dos dueños puede apagar; ninguno puede encender solo. Eso es peor que una precedencia mal elegida, y es lo que D-10 corrige. | — |
| U-06 | Comportamiento del overlay de resultados cuando el análisis está obsoleto. | **RESUELTO — no puede haber evidencia caducada** | La obsolescencia no es una bandera sobre un resultado vivo: `invalidateAnalysis` pone el análisis a `null`, y `stale` se deriva de `analysis === null && hadAnalysis`. Todos los overlays exigen `analysis?.success`, así que desaparecen en el mismo instante. Es una propiedad fail-closed estructural y hay que conservarla explícitamente en cualquier rediseño. | — |
| U-07 | Rendimiento del datasheet y de la paleta con modelos grandes (el generador admite hasta 2 000 entidades). | **ABIERTO** | Condiciona si «lista de objetos» escala como ruta de precisión y si el dock de datos densos es viable donde CB lo permite. CRI-9 no apoya ninguna decisión en que escale: la ruta de precisión canónica es el picker sobre el lienzo, no la lista. | Medición con un modelo generado de 2 000 entidades: tiempo hasta primera fila interactiva, coste de un cambio de faceta y de una selección de rango. Reproducible con el generador ya existente. |
| U-08 | Interactividad real del minimapa por teclado y por tap. | **RESUELTO — es un botón, con una sola acción** | El minimapa ES un `<button>` con `aria-label` y `onClick={onFit}`: alcanzable por teclado y por tap. Pero su única acción es encuadrar; no permite desplazarse pulsando una zona. A cambio ocupa 144px del borde derecho y descuadra el margen de encuadre (F-07). CRI-9 lo mantiene como chrome flotante DEGRADABLE: es el primero que se retira cuando CB-3 se incumple. | — |
| U-09 | ¿Qué se conserva al entrar y salir de Space 3D? | **RESUELTO** | El dominio 2D sobrevive: `ProjectProvider` está por encima del cambio de pantalla. La presentación 2D NO sobrevive: `WorkspaceShell` se desmonta entero (superficie abierta, detents, foco, cámara). Las capas del lienzo sí, porque ya se persisten. Y 3D nunca escribe en el proyecto 2D: `sourceProject` es de sólo lectura y la divergencia se DETECTA (`space3dMatchesPlanarSource`), no se propaga. Consecuencia contractual: navegar a 3D con borradores sucios exige confirmación (TR-10). | — |
| U-10 | ¿Pueden divergir localStorage e IndexedDB, y cuál manda en la recuperación? | **RESUELTO** | Sí pueden divergir, y el código ya declara qué pasa. localStorage es la autoridad de la SESIÓN VIVA: es de donde arranca el proyecto. IndexedDB es la autoridad de la BIBLIOTECA. Cuando IndexedDB ya contiene otra revisión, la copia compatible se guarda como `recovery` EN LA MISMA TRANSACCIÓN, antes de lanzar el conflicto, y se bloquean las escrituras a IndexedDB para ese id hasta que se abra una versión desde el hub. No hay pérdida silenciosa; lo que falta es la puerta desde la mesa, que abre D-08. | — |
| U-11 | Disponibilidad de `navigator.clipboard.readText()` en la matriz de navegadores del producto. | **ABIERTO — nuevo en CRI-9** | Condiciona la ruta táctil de pegado de bloque en el datasheet (D-07, DAT-06). Si no está disponible, hace falta una alternativa declarada (pegar en un campo de texto intermedio); lo que no es aceptable es que la función simplemente no exista en táctil sin decirlo. | Comprobación en el gate de WebKit ya existente (`npm run qa:webkit`) más Chromium y Firefox, con y sin contexto seguro. |
| U-12 | Coste real de la migración de `settings.show*` fuera de `ProjectSettings`. | **ABIERTO — nuevo en CRI-9** | Es el mayor riesgo adyacente a schema de todo CRI-9 (D-10). Toca `types.ts`, `migrate.ts` y el baseline protegido. | Inventario de lecturas de `settings.show*`, plan de migración con versión de esquema, y una pasada de `npm run verify:protected` sobre el resultado. Repliegue declarado en D-10 si el coste no compensa. |
| U-13 | Umbral de histéresis y ventana de estabilidad del resolutor. | **ABIERTO — nuevo en CRI-9** | T-INV-5 exige histéresis pero no fija su valor. Demasiado poca y el layout oscila en un arrastre de split-screen; demasiada y la composición se siente pegajosa. | Prototipo de CRI-11 con arrastre continuo entre 900 y 1300px midiendo recomposiciones por segundo. Es una medición, no una opinión. |
| U-14 | ¿Aula tolera que los resultados dejen de tener superficie residente? | **ABIERTO — nuevo en CRI-9, el riesgo más importante de D-03** | D-03 retira la banda de resultados de 54px en Compact y los 320px en Expanded. En Aula el bucle Resolver→leer es la tarea central. Si el resultado deja de estar a la vista, el bucle pedagógico puede romperse. | CRI-11 debe prototipar el bucle completo con la audiencia de Aula por separado, midiendo tiempo hasta el primer resultado leído. Si falla, el repliegue declarado es que en modo Aula el estado de análisis se acompañe de una tarjeta de resultado en el `inset` de la guía, sin reintroducir el panel monolítico. |
| U-15 | Contraste medido por píxel y prueba con lectores de pantalla reales. | **ABIERTO — heredado de CRI-7, no cubierto aquí** | D-14 fija el contrato de exposición de causa; no verifica que los tonos actuales lo cumplan. | Medición por píxel en ambos temas y una pasada con lector de pantalla real. Pertenece a CRI-10 y a la fase de implementación. |

## TABLA M — Frontera Expanded↔Medium calculada

Ningún umbral está escrito: todos salen de las reglas CB.

| Altura del viewport | Expanded desde |
|---|---|
| 720 px | 1130 px de ancho |
| 768 px | 1117 px de ancho |
| 800 px | 1109 px de ancho |
| 834 px | 1102 px de ancho |
| 900 px | 1089 px de ancho |
| 960 px | 1080 px de ancho |
| 1024 px | 1072 px de ancho |
| 1080 px | 1065 px de ancho |
| 1200 px | 1054 px de ancho |
| 1366 px | 1042 px de ancho |

## TABLA N — Reparto de las 122 tareas por superficie

| Superficie | Tareas |
|---|---|
| `canvas` | 19 |
| `dense` | 13 |
| `topbar` | 11 |
| `detail` | 11 |
| `welcome` | 8 |
| `status` | 8 |
| `view` | 7 |
| `layout-intent` | 6 |
| `transient` | 5 |
| `doctor` | 5 |
| `contextual-actions` | 5 |
| `analysis-setup` | 4 |
| `preferences` | 4 |
| `recovery` | 3 |
| `space3d` | 3 |
| `output` | 3 |
| `toolrail` | 3 |
| `palette` | 2 |
| `classroom` | 2 |

## TABLA O — Dueños de estado (leyenda)

| Clave | Dueño |
|---|---|
| domain | A · Dominio/persistido — ProjectModelContext |
| analysis | B · Análisis — ProjectAnalysisContext |
| selection | C · Selección/contexto — WorkspaceUIContext + focus-object |
| layout | D · Presentación/layout — LayoutIntent + Resolutor + Broker |
| workflow | E · Efímero/workflow — DraftLifecycle + estado local de superficie |
