import { ToolBar, type ToolBarProps } from './ToolBar';

/** Named Phase 7 boundary; ToolBar remains as a compatibility export. */
export const ToolRail = (props: ToolBarProps) => <ToolBar {...props} />;

