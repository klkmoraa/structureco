# Fase 3 — evolución y certificación numérica

**Fecha:** 2026-08-09 12:56 (America/Mexico_City)
**Versión:** 0.8.2
**HEAD inicial:** `99b1fa479a3bf2fce2fa3a3e0d5a3fad953c2dbe`
**Alcance:** F3-ENG-001 → F3-REL-001 → F3-PDELTA-002 → F3-SPARSE-001
**Plan ejecutado:** `docs/superpowers/plans/structureco-fase-3-plan.md`

## Resumen

Se certificó el motor real sin reescribirlo ni ampliar teoría. Primer orden conserva el backend denso como referencia y fallback. P-Delta continúa opt-in y ahora fuerza LU denso durante toda su corrida, queda marcado como experimental y no se mezcla con sparse. El backend híbrido CSR/RCM/LDLᵀ conserva sus gates y añade trazabilidad de elección/fallback. La calidad numérica se presenta como un contrato de cinco estados en Resultados, PDF y portable, sin afirmaciones de seguridad estructural.

No se cambiaron formulaciones estructurales, tolerancias físicas, unidades, signos, ejes, GDL, IDs, topología, restricciones, persistencia, undo/redo, diagramas ni formatos base. No se actualizaron dependencias ni se hizo push.

## Estado inicial y respaldo

- Rama: `main`.
- Cambios locales previos preservados y excluidos del commit F3: `src/data/modelOperations.ts`, `.worktrees/`, `reports/2026-08-09-0158-lotes-03-04-05-investigacion.md` y `reports/2026-08-09-0222-auditoria-manual-lotes-03-04-05.md`.
- `src/engine/solver.ts` aparecía modificado por estado/fin de línea, pero no tenía diff de contenido antes de F3; los cambios de contenido actuales sí pertenecen a F3.
- Baseline protegida inicial: 29 archivos verificados.
- Respaldo focalizado: `C:\Users\crisd\OneDrive\Imágenes\Escritorio\structureCo-backup-F3-2026-08-09-122401`.
- Manifiesto: 98 archivos; SHA-256 del manifiesto `10c991a50a6512879ab3773620aae415c94d52153d6bde8625c0aa6ec1a91c4e`.

## Capacidades conservadas

- Método de rigidez 2D y formulaciones Euler-Bernoulli/Timoshenko existentes.
- Ensamblaje, KKT, equilibrado simétrico, LU con pivoteo escalado, refinamiento y estimación de condición.
- CSR inferior, RCM, LDLᵀ y fallback LU existentes.
- Restricciones simples y generales, vínculos rígidos, releases, offsets, resortes, asentamientos y combinaciones.
- Recuperación N/V/M, deformada, cierres de diagrama, influence lines y auditoría independiente de cargas.
- Primer orden como modo por defecto e independiente.
- Worker y fallback síncrono sobre el mismo dispatcher de modo de análisis.

## F3-ENG — motor de primer orden

**Estado: HECHO.**

La certificación inicial ejecutó 12 archivos y 106 pruebas de solver, math, units, validation, advanced analysis, connections, load audit, diagramas, influence, invariantes, FTool y protocolo de worker. No apareció una divergencia de formulación reproducible, por lo que no se cambió teoría ni recuperación estructural.

La única modificación del solver de primer orden es contractual: propaga una política interna de backend a condensación, resolución global y recuperación de conexiones, y publica qué backend resolvió la corrida. Con política omitida se conserva el comportamiento híbrido actual; LU denso sigue siendo baseline y fallback.

## F3-REL — calidad numérica

**Estado: HECHO.**

- Se añadió el estado público `stable | limited | unreliable | failed | unavailable`, derivado de `completed`, `usable` y la clasificación existente.
- Se conservan como controles: condición κ₁, residuo lineal, residuo estructural, refinamiento, error hacia adelante, dígitos estimados, restricciones, equilibrio, load audit, cierres, compatibilidad y convergencia P-Delta.
- Resultados muestra un bloque semántico de calidad con estado, métricas, backend, control gobernante, causas y límite interpretativo.
- Estados fallidos conservan su resultado/diagnóstico visible y acceso a avisos.
- PDF reemplaza la afirmación incorrecta `EQUILIBRIO APROBADO` por `CALIDAD NUMERICA`, valores medidos y la advertencia de que no evalúa seguridad estructural.
- Portable incorpora metadata aditiva `numericQuality`, `analysisMode` y `pDeltaExperimental`; el `AnalysisResult` completo continúa incluido y protegido por checksum.
- El copy ES/EN está separado de los valores. No se usan “seguro”, factor de seguridad, Pf, β, FORM ni SORM.

## F3-PDELTA — delimitación experimental

**Estado: IMPLEMENTADO Y VALIDADO COMO EXPERIMENTAL.**

- `analysisMode: 'first-order'` continúa como default; P-Delta sigue siendo selección explícita.
- La corrida P-Delta fuerza `linearBackend: 'dense'` en el primer orden de referencia, cada paso, cada iteración, condensación de conexiones y recuperación.
- `PDeltaDiagnostics.experimental` se publica tanto en convergencia como en configuración inválida, fallo inicial y no convergencia.
- La UI y el PDF identifican P-Delta como experimental.
- El factor crítico permanece descrito como estimación acotada por discretización, nunca como carga crítica exacta ni como análisis de valores propios.
- Worker, fallback y exportación sin análisis previo preservan el modo P-Delta y la política densa; se eliminó el recálculo silencioso de primer orden al exportar.

No se modificó la matriz de rigidez geométrica ni su convención de signos.

## F3-SPARSE — cierre experimental

**Estado: IMPLEMENTADO Y VALIDADO COMO EXPERIMENTAL INTERNO.**

- `solveLinearSystem` acepta una política interna aditiva `auto | dense`.
- Se reporta `dense-lu | sparse-ldlt` y la causa de fallback: denso forzado, tamaño insuficiente, restricciones no reducibles, bloque reducido pequeño, fill-in excesivo o pivote no positivo.
- Una restricción multi-GDL se identifica en el gate de aplicabilidad antes de intentar LDLᵀ; el resultado ya caía a LU denso, por lo que sólo se corrigió la causa diagnóstica.
- La comparación estructural denso/híbrido cubre desplazamientos, reacciones, N/V/M, load audit, equilibrio, residual y determinismo.
- No se agregó selector persistente, no se cambió el backend por defecto y no se elevaron gates.

## Contratos y formulaciones modificados

Contratos aditivos:

- `AnalysisResult.linearSolver?: LinearSolverDiagnostics`.
- `PDeltaDiagnostics.experimental: true`.
- `NumericQualityState` de cinco estados.
- `AnalyzeProjectOptions.linearBackend?: 'auto' | 'dense'`.
- Tercer argumento opcional de `solveLinearSystem` para política de backend.
- Metadata portable de calidad/modo/experimentalidad.

Formulaciones modificadas: **ninguna**. No cambiaron rigidez elástica, rigidez geométrica, KKT, equilibrado, signos, ejes, cargas equivalentes, recuperación ni diagramas.

Rutas protegidas modificadas intencionalmente y autorizadas:

- `src/engine/math.ts`
- `src/engine/solver.ts`
- `src/engine/reliability.ts`
- `src/engine/pDelta.ts`
- `src/types.ts`

La baseline `scripts/protected-baseline.sha256` se actualizó sólo después de las pruebas focalizadas y volvió a verificar 29 archivos.

## Casos manuales y oráculos

Casos ejecutados mediante fixtures deterministas:

- Viga simplemente apoyada: uniforme, triangular y parcial.
- Voladizo: carga puntual y carga uniforme.
- Pórtico y barra inclinada con carga global.
- Resorte axial, release de extremo, vínculo rígido y rodillo inclinado.
- Asentamiento prescrito, combinación y superposición.
- Mecanismo y restricciones incompatibles/no reducibles.
- Cargas puntuales, distribuidas, trapezoidales, por proyección y peso propio.
- P-Delta con axial bajo, proximidad crítica, reducción de paso, agotamiento y estado postcrítico/no convergente.
- Modelos grandes con restricción simple para sparse y modelos KKT no reducibles para fallback.

Oráculos usados:

- Soluciones cerradas ya codificadas de vigas y voladizos.
- Solución cerrada de viga-columna en voladizo y referencia de Euler ya presentes.
- Fixtures comparativos FTool existentes.
- Equilibrio global, reciprocidad de Maxwell-Betti, homogeneidad e invariantes.
- Load audit independiente, cierre N/V/M y compatibilidad de deformada.
- Comparación dense/hybrid y repetición determinista.

## Tolerancias

- Se conservaron todas las bandas existentes de reliability y de cada familia de pruebas.
- No se relajó ningún threshold.
- La regresión dense/hybrid usa paridad relativa `2e-10`, load audit `≤ 1e-10`, equilibrio `≤ 1e-8` y residual híbrido no mayor que `max(1e-14, 10 × residual denso)`.
- Las tolerancias P-Delta permanecen las justificadas por los benchmarks analíticos y de discretización existentes.

## Pruebas y validación

TDD focalizado:

- Rojo inicial: 7 archivos, 9 fallos esperados (traza sparse, cinco estados, aislamiento P-Delta, UI, PDF, portable y export dispatcher).
- Verde consolidado: 8 archivos, 90 aprobadas y 3 skips históricos.
- Validación focalizada final: 32 archivos, 317 aprobadas y 3 skips históricos.
- Worker P-Delta: paridad estructurada con fallback, metadata experimental y LU denso.

Gates individuales finales:

- `npm.cmd run lint`: aprobado, repetido después de la corrección final.
- `npm.cmd run typecheck`: aprobado, repetido después de la corrección final.
- `npm.cmd run verify:protected`: aprobado, 29 archivos.
- `npm.cmd run build`: aprobado después de la corrección final; 2,501 módulos.
- `npm.cmd run verify:perf`: aprobado; 705,657 bytes / 187,521 gzip frente a 722,000 / 192,000.
- `git diff --check`: aprobado; sólo avisos informativos LF→CRLF.

`npm.cmd run verify` se ejecutó una sola vez. Llegó a 793 pruebas aprobadas y 3 skips, pero terminó con dos fallos: el detector de política numérica encontró un `toFixed` nuevo de F3 y un test histórico de `App` excedió por 257 ms su timeout de 5 s. La causa F3 se corrigió con `formatFixed` y su suite aislada pasó 4/4. El test de `App` pasó aislado en 3.89 s (1/1), demostrando contención de la corrida integral; conforme a la regla de no repetir toda la suite, sólo se repitieron las comprobaciones relacionadas. Build, perf, lint y typecheck volvieron a pasar después.

Validación visual:

- Navegador real: escritorio y móvil 390×844, temas oscuro y claro, sin overflow horizontal ni errores de consola.
- PDF: A4, 12 páginas, portada renderizada a PNG sin recortes ni superposiciones; bloque de calidad legible.

## Benchmarks F3-SPARSE

Fixture reproducible: viga continua, `includeEducationTrace: false`, backend `sparse-ldlt`.

| Miembros | Total | Assembly | Solve | Diagramas | Deformada | Audit/reliability | Residual | Condición | Δ heap observado |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 100 | 309.9 ms | 42.23 ms | 21.31 ms | 37.73 ms | 102.17 ms | 9.09 ms | 6.298e-18 | 4.325e+5 | 74.5 MiB |
| 500 | 1,745.8 ms | 726.65 ms | 228.54 ms | 28.05 ms | 368.38 ms | 26.37 ms | 5.897e-18 | 2.155e+6 | 290.8 MiB |
| 1,000 | 6,569.1 ms | 4,351.10 ms | 767.64 ms | 43.56 ms | 797.19 ms | 44.16 ms | 5.897e-18 | 4.309e+6 | 816.9 MiB |

Los datos justifican conservar sparse como experimental interno: a 1,000 miembros el ensamblaje ya consume 66.2% del tiempo y el incremento de heap observado es ~817 MiB.

## Mejoras extras

- **MEJORA EXTRA IMPLEMENTADA:** dispatcher testeable para exportación portable que respeta el modo configurado y evita sustituir P-Delta por primer orden.
- **MEJORA EXTRA IMPLEMENTADA:** eliminación del copy `EQUILIBRIO APROBADO` en PDF, que confundía éxito de resolución con aprobación estructural.
- **MEJORA EXTRA IMPLEMENTADA:** causas explícitas de fallback sparse sin alterar valores publicados.
- **PROPUESTA PARA DESPUÉS:** assembly directo triplet/CSR y medición real de memoria pico antes de considerar 5k+.

## Componentes experimentales y limitaciones

- P-Delta 2D sigue experimental, opt-in y no certificado.
- Sparse híbrido sigue interno y experimental; siempre conserva fallback LU denso.
- 5k+ queda bloqueado arquitectónicamente: assembly materializa `number[][]` antes de CSR. No se implementó assembly sparse completo.
- La calidad numérica no es seguridad estructural, factor de seguridad, confiabilidad probabilística ni cumplimiento normativo.
- No se implementaron P-δ, eigen-buckling, plasticidad, material no lineal, grandes desplazamientos, 3D, IA ni cambios generales de trace.
- No se cambiaron dependencias, persistencia ni formatos base; la metadata portable es aditiva bajo format version 1.

## Defectos aislados

- `src/App.test.tsx` puede exceder el timeout fijo de 5 s bajo la corrida integral concurrente; pasó aislado en 3.89 s. No se modificó código ajeno para ocultar la contención.
- Poppler informó falta de fuente de display `Symbol` al renderizar la muestra PDF; las fórmulas existentes se inspeccionaron visualmente y aparecieron legibles. No pertenece a Fase 3.
- Los avisos LF→CRLF de Git son informativos; `git diff --check` devuelve código 0.

## Rollback

1. Revertir únicamente el commit local `feat: implement structureCo phase 3 evolution`.
2. Si se requiere restauración de archivos, usar el respaldo focalizado y verificar primero el manifiesto SHA-256.
3. Reponer `scripts/protected-baseline.sha256` junto con las cinco rutas protegidas; no restaurar ni revertir los cambios locales previos del usuario.
4. Primer orden denso conserva independencia funcional como baseline/fallback.

## Pendientes y bloqueos

- **BLOQUEADO PARA ESTA FASE:** 5k+ y escalabilidad masiva hasta eliminar assembly `number[][]` y demostrar memoria, tiempo, fill-in, cancelación y rollback.
- **NO HACER TODAVÍA:** activar P-Delta o sparse por defecto, certificar capacidad, o mezclar ambos backends.
- No existe un archivo de seguimiento Fase 3 adicional en la aplicación raíz; el plan y este reporte son el seguimiento único actualizado.

## Archivos F3

- Plan/reporte: `docs/superpowers/plans/structureco-fase-3-plan.md`, este archivo.
- Contratos/motor: `src/types.ts`, `src/engine/math.ts`, `src/engine/solver.ts`, `src/engine/reliability.ts`, `src/engine/pDelta.ts`.
- Pruebas/benchmarks: `src/engine/math.test.ts`, `src/engine/reliability.test.ts`, `src/engine/pDelta.test.ts`, `src/engine/sparseBackend.test.ts`, `src/engine/benchmarks.test.ts`, `src/engine/analysisWorkerProtocol.test.ts`.
- UI/i18n: `src/features/results/NumericQualityCard.tsx`, `src/features/results/ResultSummary.tsx`, `src/features/results/ResultsPanel.tsx`, `src/features/results/ResultsPanel.test.tsx`, `src/i18n/catalogs.ts`, `src/styles.css`.
- Exportaciones: `src/features/topbar/portableExportAnalysis.ts`, `src/features/topbar/portableExportAnalysis.test.ts`, `src/features/topbar/TopBar.tsx`, `src/utils/portableTypes.ts`, `src/utils/portablePayload.ts`, `src/utils/portable.test.ts`, `src/utils/pdf/pdfCover.ts`, `src/utils/calculationPdfEditorial.test.ts`.
- Protección: `scripts/protected-baseline.sha256`.

## Cómo verificar

```powershell
npx.cmd vitest run --maxWorkers=1 src/engine/solver.test.ts src/engine/math.test.ts src/engine/units.test.ts src/engine/validation.test.ts src/engine/advancedAnalysis.test.ts src/engine/connections.test.ts src/engine/loadAudit.test.ts src/engine/diagram.test.ts src/engine/influence.test.ts src/engine/analysisWorkerProtocol.test.ts src/runtime/workerProtocol.test.ts src/engine/pDelta.test.ts src/engine/pDeltaBenchmarks.test.ts src/engine/reliability.test.ts src/engine/sparseBackend.test.ts src/engine/benchmarks.test.ts src/engine/invariants.test.ts src/engine/ftoolComparison.test.ts src/data/migrate.test.ts src/features/results/ResultsPanel.test.tsx src/features/topbar/portableExportAnalysis.test.ts src/utils/calculationPdfEditorial.test.ts src/utils/portable.test.ts
npm.cmd run lint
npm.cmd run typecheck
npm.cmd run verify:protected
npm.cmd run build
npm.cmd run verify:perf
git diff --check
```
